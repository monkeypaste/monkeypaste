
namespace MonkeyPaste {
    public enum MpDragDropOpType {
        None = 0,
        dragstart,
        dragend,
        dragenter,
        dragover,
        dragleave,
        drop,
        reset
    }
}
