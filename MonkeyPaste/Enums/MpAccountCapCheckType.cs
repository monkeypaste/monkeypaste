namespace MonkeyPaste {
    public enum MpAccountCapCheckType {
        None = 0,
        Add,
        Link,
        RestoreBlock,
        AddBlock,
        Remove,
        Init,
        Refresh
    }
}
