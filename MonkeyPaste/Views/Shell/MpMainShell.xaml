<Shell x:Class="MonkeyPaste.MpMainShell"
       xmlns="http://xamarin.com/schemas/2014/forms"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
       xmlns:local="clr-namespace:MonkeyPaste"
       x:DataType="local:MpMainShellViewModel"
       Title="Monkey Paste">
    <Shell.FlyoutContentTemplate>
        <DataTemplate>
            <StackLayout>
                <Grid Margin="10"
                      HeightRequest="100">
                    <Image Source="{StaticResource AppIcon}"
                           HorizontalOptions="Center"
                           VerticalOptions="Center"
                           Margin="20" />
                </Grid>
                <local:MpTagCollectionView BindingContext="{Binding TagCollectionViewModel}" />
                <Grid ColumnDefinitions="0.2*,0.8*">
                    <Image Source="{StaticResource SettingsIcon}"
                           Margin="5"
                           HeightRequest="45" />
                    <Label Grid.Column="1"
                           Text="Settings"
                           TextColor="Black"
                           FontAttributes="Italic"
                           VerticalTextAlignment="Center" />
                </Grid>
                <Grid ColumnDefinitions="0.2*,0.8*">
                    <Image Source="{StaticResource HelpIcon}"
                           Margin="5"
                           HeightRequest="45" />
                    <Label Grid.Column="1"
                           Text="Help"
                           TextColor="Black"
                           FontAttributes="Italic"
                           VerticalTextAlignment="Center" />
                </Grid>
            </StackLayout>            
        </DataTemplate>
    </Shell.FlyoutContentTemplate>
    <FlyoutItem x:Name="FlyoutItem"
                FlyoutDisplayOptions="AsMultipleItems">
        <ShellContent x:Name="CollectionsContent"
                      Title="Collections"
                      Icon="{StaticResource TagsIcon}"
                      ContentTemplate="{DataTemplate local:MpMainView}" />
    </FlyoutItem>
</Shell>
