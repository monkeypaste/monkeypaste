<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ff="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
             xmlns:local="clr-namespace:MonkeyPaste"
             x:Class="MonkeyPaste.MpCopyItemDetailPageView"
             x:DataType="local:MpCopyItemViewModel">
    <ContentPage.Content>        
        <Frame IsClippedToBounds="True"
               HasShadow="True"
               Padding="0"
               BackgroundColor="White">
            <Frame.OutlineColor>
                <OnPlatform x:TypeArguments="Color"
                            Android="Gray"
                            iOS="Gray" />
            </Frame.OutlineColor>
            <Frame.Margin>
                <OnPlatform x:TypeArguments="Thickness"
                            Android="10"
                            iOS="10" />
            </Frame.Margin>
            <StackLayout Orientation="Horizontal">
                <BoxView Style="{StaticResource CopyItem_Selection_BoxView}"
                         WidthRequest="6" />
                <Image Aspect="AspectFit"
                       WidthRequest="60"
                       HeightRequest="60"
                       Margin="0,10,0,0"
                       VerticalOptions="Start">
                    <Image.Source>
                        <local:MpPackageNameSource PackageName="{Binding CopyItem.Host}" />
                    </Image.Source>
                </Image>
                <Grid VerticalOptions="CenterAndExpand"
                      Padding="0"
                      HorizontalOptions="FillAndExpand"
                      BackgroundColor="Transparent">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Label FontAttributes="Bold"
                           TextColor="Black"
                           Grid.Row="0"
                           HorizontalTextAlignment="Start"
                           VerticalTextAlignment="Center"
                           FontSize="16"
                           Text="{Binding CopyItem.Title, Mode = TwoWay}">
                        <Label.LineBreakMode>
                            <OnPlatform x:TypeArguments="LineBreakMode"
                                        Android="NoWrap"
                                        iOS="TailTruncation" />
                        </Label.LineBreakMode>
                    </Label>
                    <BoxView Grid.Row="1"
                             Color="Gray"
                             HorizontalOptions="FillAndExpand"
                             HeightRequest="1" />
                    <Label Grid.Row="2"
                           TextColor="Black"
                           FontSize="16"
                           Text="{Binding CopyItem.ItemPlainText, Mode=TwoWay}" />
                </Grid>
            </StackLayout>
        </Frame>
    </ContentPage.Content>
</ContentPage>