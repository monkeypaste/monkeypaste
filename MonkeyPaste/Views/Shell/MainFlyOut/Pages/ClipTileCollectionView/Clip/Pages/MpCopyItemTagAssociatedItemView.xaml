<?xml version="1.0" encoding="UTF-8" ?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MonkeyPaste"
             x:Class="MonkeyPaste.MpCopyItemTagAssociatedItemView"
             x:DataType="local:MpCopyItemTagAssociatedItemViewModel">
    <ContentView.Content>
        <Frame Margin="0"
               Padding="20"
               BorderColor="Black"
               BackgroundColor="DimGray"
               IsClippedToBounds="True">
            <Frame.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding ToggleAssociationCommand}" />
            </Frame.GestureRecognizers>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="0.2*" />
                    <ColumnDefinition Width="0.8*" />
                </Grid.ColumnDefinitions>
                <Image Grid.Column="0"
                       BackgroundColor="Transparent"
                       Aspect="AspectFit"
                       WidthRequest="40"
                       HeightRequest="40"
                       HorizontalOptions="Center"
                       VerticalOptions="Center">
                    <Image.Triggers>
                        <DataTrigger TargetType="Image"
                                     Binding="{Binding IsAssociated}"
                                     Value="True">
                            <Setter Property="Source">
                                <Setter.Value>
                                    <FontImageSource FontFamily="{StaticResource FontAwesomeRegular}"
                                                     Color="{Binding Tag.HexColor,
                                                                     Converter={StaticResource StringToColorConverter}}"
                                                     Glyph="&#xf14a;" />
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>
                        <DataTrigger TargetType="Image"
                                     Binding="{Binding IsAssociated}"
                                     Value="False">
                            <Setter Property="Source">
                                <Setter.Value>
                                    <FontImageSource FontFamily="{StaticResource FontAwesomeRegular}"
                                                     Color="{Binding Tag.HexColor,
                                                                     Converter={StaticResource StringToColorConverter}}"
                                                     Glyph="&#xf0c8;" />
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>
                    </Image.Triggers>
                </Image>
                <Label Grid.Column="1"
                       Text="{Binding Tag.TagName}"
                       TextColor="Black"
                       VerticalOptions="Center"
                       VerticalTextAlignment="Center"
                       FontSize="18">
                    <Label.Triggers>
                        <DataTrigger TargetType="Label"
                                     Binding="{Binding IsEnabled}"
                                     Value="True">
                            <Setter Property="FontAttributes"
                                    Value="None" />
                        </DataTrigger>
                        <DataTrigger TargetType="Label"
                                     Binding="{Binding IsEnabled}"
                                     Value="False">
                            <Setter Property="FontAttributes"
                                    Value="Italic" />
                        </DataTrigger>
                        <DataTrigger TargetType="Label"
                                     Binding="{Binding IsEnabled}"
                                     Value="False">
                            <Setter Property="TextColor"
                                    Value="Gray" />
                        </DataTrigger>
                    </Label.Triggers>
                </Label>
            </Grid>
        </Frame>
    </ContentView.Content>
</ContentView>