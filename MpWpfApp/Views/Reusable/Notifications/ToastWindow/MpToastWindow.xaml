<local:MpWindow xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:local="clr-namespace:MpWpfApp"
                xmlns:pcl="clr-namespace:MonkeyPaste;assembly=MonkeyPaste"
                xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                x:TypeArguments="pcl:MpNotificationBalloonViewModel"
                x:Class="MpWpfApp.MpToastWindow"
                x:Name="uiMainNotifyWindow"
                Title=""
                
                MinHeight="125"
                Width="350"
                ShowInTaskbar="False"
                WindowStyle="None"
                AllowsTransparency="True"
                ResizeMode="NoResize"
                Background="Transparent"
                Visibility="Visible"
                Topmost="True"
                BorderThickness="1"
                mc:Ignorable="d"
                SizeToContent="WidthAndHeight"
                Loaded="uiMainNotifyWindow_Loaded"
                d:DataContext="{d:DesignInstance Type=pcl:MpNotificationBalloonViewModel,
                                                 IsDesignTimeCreatable=False}">
    <Window.Resources>
        <Storyboard x:Key="gridFadeInStoryBoard">
            <DoubleAnimation Storyboard.TargetName="uiGridMain"
                             BeginTime="0:0:0.1"
                             Storyboard.TargetProperty="Opacity"
                             From="0.00"
                             To="1.00"
                             AutoReverse="False"
                             Duration="0:0:0.3" />
        </Storyboard>
        <Storyboard x:Key="gridFadeOutStoryBoard">
            <DoubleAnimation Storyboard.TargetName="uiGridMain"
                             BeginTime="0:0:0.5"
                             Storyboard.TargetProperty="Opacity"
                             From="1.00"
                             To="0.00"
                             AutoReverse="False"
                             Duration="0:0:0.3" />
        </Storyboard>
        <Storyboard x:Key="tommyTest">
            <DoubleAnimation Storyboard.TargetName="uiGridMain"
                             BeginTime="0:0:0.5"
                             Storyboard.TargetProperty="Opacity"
                             From="1.00"
                             To="0.00"
                             AutoReverse="False"
                             Duration="0:0:0.3" />
        </Storyboard>
    </Window.Resources>
    <!--<Window.RenderTransform>
        <TranslateTransform x:Name="WindowTranslateTransform" />
    </Window.RenderTransform>-->
    <local:MpNotificationBalloonView x:Name="NotificationBalloon" 
                                     DataContext="{Binding}" />
</local:MpWindow>
