<local:MpUserControl x:Class="MpWpfApp.MpSearchCriteriaListBoxView"
                     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                     xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                     xmlns:dd="clr-namespace:GongSolutions.Wpf.DragDrop;assembly=GongSolutions.Wpf.DragDrop"
                     xmlns:local="clr-namespace:MpWpfApp"
                     x:TypeArguments="local:MpSearchBoxViewModel"
                     mc:Ignorable="d"
                     d:DesignHeight="450"
                     d:DesignWidth="800"
                     d:DataContext="{d:DesignInstance Type=local:MpSearchBoxViewModel,
                                                      IsDesignTimeCreatable=False}">
    <ListBox x:Name="SearchCriteriaListBox"
             PreviewMouseWheel="SearchCriteriaListBox_PreviewMouseWheel"
             Height="{Binding SearchCriteriaListBoxHeight}"
             RequestBringIntoView="SearchCriteriaListBox_RequestBringIntoView"
             Background="{local:MpThemeColorsBinding Key=Filter_Menu_Background_Color}"
             dd:DragDrop.IsDragSource="True"
             dd:DragDrop.IsDropTarget="True"
             BorderThickness="0"
             BorderBrush="Black"
             ItemsSource="{Binding CriteriaItems}"
             SelectionMode="Single"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch"
             HorizontalContentAlignment="Stretch"
             VerticalContentAlignment="Stretch"
             ScrollViewer.VerticalScrollBarVisibility="Hidden"
             ScrollViewer.HorizontalScrollBarVisibility="Hidden">
        <ListBox.ItemContainerStyle>
            <Style TargetType="{x:Type ListBoxItem}" >
                <Setter Property="IsSelected"
                        Value="{Binding IsSelected}" />
                <Setter Property="FocusVisualStyle"
                                        Value="{x:Null}" />
                <Setter Property="HorizontalContentAlignment"
                        Value="Left" />
                <Setter Property="VerticalAlignment"
                        Value="Stretch" />
                <Setter Property="VerticalContentAlignment"
                        Value="Stretch" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ListBoxItem}">
                            <ContentPresenter />
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ListBox.ItemContainerStyle>
        <ListBox.ItemTemplate>
            <DataTemplate>
                <local:MpSearchCriteriaItemView Background="{Binding CriteriaItemBackgroundBrush}"
                                                BorderThickness="0"
                                                Height="{Binding Parent.SearchCriteriaListBoxItemHeight}"
                                                BorderBrush="{Binding CriteriaItemBorderBrush}"
                                                DataContext="{Binding}" />
            </DataTemplate>
        </ListBox.ItemTemplate>
    </ListBox>
</local:MpUserControl>
