<local:MpUserControl x:Class="MpWpfApp.MpTagTabsView"
                     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
                     xmlns:dd="clr-namespace:GongSolutions.Wpf.DragDrop;assembly=GongSolutions.Wpf.DragDrop"
                     xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                     xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                     xmlns:local="clr-namespace:MpWpfApp"
                     x:TypeArguments="local:MpTagTrayViewModel"
                     mc:Ignorable="d"
                     d:DataContext="{d:DesignInstance Type=local:MpTagTrayViewModel,
                                                      IsDesignTimeCreatable=False}"
                     d:DesignHeight="450"
                     d:DesignWidth="800">
    <Grid x:Name="TagTrayContainerGrid"
          PreviewMouseDown="TagTrayContainerGrid_PreviewMouseDown"
          Focusable="False"
          HorizontalAlignment="Center">
        <Grid.Resources>
            <local:ContentToMarginConverter x:Key="ContentToMarginConverter" />
            <local:ContentToPathConverter x:Key="ContentToPathConverter" />

            <SolidColorBrush x:Key="BorderBrush"
                             Color="#FFFFFFFF" />
            <SolidColorBrush x:Key="HoverBrush"
                             Color="#FFFF4500" />
            <SolidColorBrush x:Key="TabControlBackgroundBrush"
                             Color="Transparent" />
            <!--<LinearGradientBrush x:Key="TabItemPathBrush"
                                 StartPoint="0,0"
                                 EndPoint="0,1">
                <GradientStop Color="#FF3164a5"
                              Offset="0" />
                <GradientStop Color="#FFe4f6fa"
                              Offset="1" />
            </LinearGradientBrush>-->
            <Style x:Key="TabControlStyle"
                   TargetType="{x:Type TabControl}">
                <Setter Property="BorderThickness"
                        Value="10" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TabControl">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <Border Grid.Row="1"
                                        BorderThickness="2,0,2,2"
                                        Panel.ZIndex="2"
                                        CornerRadius="0,0,2,2"
                                        BorderBrush="{StaticResource BorderBrush}">
                                    <ContentPresenter ContentSource="SelectedContent" />
                                </Border>
                                <StackPanel Orientation="Horizontal"
                                            Grid.Row="0"
                                            Panel.ZIndex="1"
                                            Margin="20,0,20,0"
                                            IsItemsHost="true" />
                                <Rectangle Grid.Row="0"
                                           Height="2"
                                           VerticalAlignment="Bottom"
                                           Fill="{StaticResource BorderBrush}" />
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <!--  TabControl style  -->

            <Style x:Key="TabItemStyle"
                   TargetType="{x:Type TabItem}">
                <Setter Property="SnapsToDevicePixels"
                        Value="True" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TabItem">
                            <Grid x:Name="grd">
                                <Path x:Name="TabPath"
                                      StrokeThickness="2"
                                      Margin="{Binding ElementName=TabItemContent,
                                                       Converter={StaticResource ContentToMarginConverter}}"
                                      Stroke="{StaticResource BorderBrush}"
                                      Fill="{Binding TagHexColor,
                                                     Converter={StaticResource StringHexToWinMediaColorConverter}}">
                                    <Path.Data>
                                        <PathGeometry>
                                            <PathFigure IsClosed="False"
                                                        StartPoint="1,0"
                                                        Segments="{Binding ElementName=TabItemContent,
                                                                           Converter={StaticResource ContentToPathConverter}}" />
                                        </PathGeometry>
                                    </Path.Data>
                                    <Path.LayoutTransform>
                                        <ScaleTransform ScaleY="-1" />
                                    </Path.LayoutTransform>
                                </Path>
                                <Rectangle x:Name="TabItemTopBorder"
                                           Height="2"
                                           Visibility="Visible"
                                           VerticalAlignment="Bottom"
                                           Fill="{StaticResource BorderBrush}"
                                           Margin="{Binding ElementName=TabItemContent,
                                                            Converter={StaticResource ContentToMarginConverter}}" />
                                <ContentPresenter x:Name="TabItemContent"
                                                  ContentSource="Header"
                                                  Margin="10,2,10,2"
                                                  VerticalAlignment="Center"
                                                  TextElement.Foreground="#FF000000" />
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver"
                                         Value="True"
                                         SourceName="grd">
                                    <Setter Property="Stroke"
                                            Value="{StaticResource HoverBrush}"
                                            TargetName="TabPath" />
                                </Trigger>
                                <Trigger Property="Selector.IsSelected"
                                         Value="True">
                                    <Setter Property="Fill"
                                            TargetName="TabPath">
                                        <Setter.Value>
                                            <SolidColorBrush Color="#FFe4f6fa" />
                                        </Setter.Value>
                                    </Setter>
                                    <Setter Property="BitmapEffect">
                                        <Setter.Value>
                                            <DropShadowBitmapEffect Direction="302"
                                                                    Opacity="0.4"
                                                                    ShadowDepth="2"
                                                                    Softness="0.5" />
                                        </Setter.Value>
                                    </Setter>
                                    <Setter Property="Panel.ZIndex"
                                            Value="2" />
                                    <Setter Property="Visibility"
                                            Value="Hidden"
                                            TargetName="TabItemTopBorder" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.2*" />
            <ColumnDefinition Width="0.1*" />
            <ColumnDefinition Width="auto"
                              MaxWidth="{Binding TagTrayDefaultMaxWidth}" />
            <ColumnDefinition Width="0.1*" />
            <ColumnDefinition Width="0.2*" />
        </Grid.ColumnDefinitions>

        <!--  Smart Filter Dropdown Button  -->

        <Button x:Name="SmartFilterDropdownButton"
                Height="30"
                Width="30"
                Grid.Column="0"
                BorderThickness="0"
                ToolTip="Add Collection"
                ToolTipService.InitialShowDelay="100"
                Template="{StaticResource SearchButtonControlTemplate}"
                Background="Transparent"
                Visibility="Collapsed">
            <Image Stretch="Fill"
                   Source="/MpWpfApp;component/Resources/Images/down_black.png" />
        </Button>
        <RepeatButton x:Name="TagTrayNavLeftButton"
                      Click="TagTrayNavLeftButton_Click"
                      Margin="5"
                      Padding="5"
                      Width="30"
                      Grid.Column="1"
                      Template="{StaticResource FlatRepeatButtonControlTemplate}"
                      Visibility="Collapsed">
            <Image Source="/Resources/Images/back_black.png"
                   Stretch="Fill" />
        </RepeatButton>
        <TabControl x:Name="TagTray"
                    Loaded="TagTray_Loaded"
                    Margin="1"
                    FontSize="16"
                    Grid.Column="2"
                    Focusable="False"
                    TabStripPlacement="Top"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    dd:DragDrop.IsDragSource="True"
                    dd:DragDrop.IsDropTarget="True"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    MaxWidth="{Binding TagTrayDefaultMaxWidth}"
                    SelectionChanged="TagTray_SelectionChanged"
                    ScrollViewer.IsDeferredScrollingEnabled="False"
                    ScrollViewer.CanContentScroll="False"
                    ScrollViewer.VerticalScrollBarVisibility="Hidden"
                    ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                    ItemsSource="{Binding PinnedItems}">
            <TabControl.ItemContainerStyle>
                <Style TargetType="TabItem">
                    <Style.Resources>
                        <SolidColorBrush Color="Transparent"
                                         x:Key="{x:Static SystemColors.HighlightBrushKey}" />
                        <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}"
                                         Color="Transparent" />
                        <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}"
                                         Color="Transparent" />
                        <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}"
                                         Color="Transparent" />
                        <SolidColorBrush x:Key="RedBrush"
                                         Color="Red" />

                        <SolidColorBrush x:Key="SolidBorderBrush"
                                         Color="#888" />

                        <SolidColorBrush x:Key="GreenBrush"
                                         Color="Green" />

                        <SolidColorBrush x:Key="DisabledBackgroundBrush"
                                         Color="#EEE" />

                        <SolidColorBrush x:Key="DisabledBorderBrush"
                                         Color="#AAA" />

                        <SolidColorBrush x:Key="DisabledForegroundBrush"
                                         Color="#888" />
                    </Style.Resources>
                    <Setter Property="Background"
                            Value="Transparent" />
                    <Setter Property="VerticalAlignment"
                            Value="Top" />
                    <Setter Property="BorderThickness"
                            Value="0" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type TabItem}">
                                <Grid>
                                    <Border Name="Border"
                                            Margin="0,0,-4,0"
                                            Background="{StaticResource RedBrush}"
                                            BorderBrush="{StaticResource SolidBorderBrush}"
                                            BorderThickness="1,1,1,1"
                                            CornerRadius="2,12,0,0">
                                        <ContentPresenter x:Name="ContentSite"
                                                          VerticalAlignment="Center"
                                                          HorizontalAlignment="Center"
                                                          ContentSource="Header"
                                                          Margin="12,2,12,2"
                                                          RecognizesAccessKey="True" />
                                    </Border>
                                </Grid>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected"
                                             Value="True">
                                        <Setter Property="Panel.ZIndex"
                                                Value="100" />
                                        <Setter TargetName="Border"
                                                Property="Background"
                                                Value="{StaticResource GreenBrush}" />
                                        <Setter TargetName="Border"
                                                Property="BorderThickness"
                                                Value="1,1,1,0" />
                                    </Trigger>
                                    <Trigger Property="IsEnabled"
                                             Value="False">
                                        <Setter TargetName="Border"
                                                Property="Background"
                                                Value="{StaticResource DisabledBackgroundBrush}" />
                                        <Setter TargetName="Border"
                                                Property="BorderBrush"
                                                Value="{StaticResource DisabledBorderBrush}" />
                                        <Setter Property="Foreground"
                                                Value="{StaticResource DisabledForegroundBrush}" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.ItemContainerStyle>
            <TabControl.ItemTemplate>
                <DataTemplate DataType="{x:Type local:MpTagTileViewModel}">
                    <local:MpTagTileView DataContext="{Binding}" />
                </DataTemplate>
            </TabControl.ItemTemplate>
        </TabControl>
        <RepeatButton x:Name="TagTrayNavRightButton"
                      Click="TagTrayNavRightButton_Click"
                      Grid.Column="3"
                      Margin="5"
                      Padding="5"
                      Width="30"
                      Template="{StaticResource FlatRepeatButtonControlTemplate}"
                      Visibility="Collapsed">
            <Image Source="/Resources/Images/forward_black.png"
                   Stretch="Fill" />
        </RepeatButton>
    </Grid>
</local:MpUserControl>
