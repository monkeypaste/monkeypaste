<UserControl x:Class="MpWpfApp.MpPasteTemplateToolbarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:local="clr-namespace:MpWpfApp"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border x:Name="ClipTilePasteTemplateToolbar"
            HorizontalAlignment="Stretch"
            Height="40"
            CornerRadius="5"
            BorderThickness="0"
            Background="LightBlue"
            Visibility="{Binding HostClipTileViewModel.IsPastingTemplate, Converter={StaticResource BoolToVisibilityConverter}}">
        <Grid x:Name="PasteTemplateToolbarGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="0.7*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <local:MpClipBorder x:Name="SelectedTemplateComboBoxBorder"
                                Grid.Column="0"
                                HorizontalAlignment="Center"
                                Background="{Binding SelectedTemplate.TemplateBrush}"
                                CornerRadius="15"
                                Margin="0,3,0,3">
                <ComboBox x:Name="SelectedTemplateComboBox"
                          FontSize="14"
                          MinWidth="100"
                          Background="Transparent"
                          HorizontalContentAlignment="Left"
                          VerticalContentAlignment="Center"
                          SelectedIndex="{Binding SelectedTemplateIdx, Mode=TwoWay, NotifyOnTargetUpdated=True, NotifyOnSourceUpdated=True, UpdateSourceTrigger=PropertyChanged}"
                          ItemsSource="{Binding Templates}">
                    <ComboBox.ItemTemplate>
                        <ItemContainerTemplate>
                            <DockPanel>
                                <Border Background="{Binding TemplateBrush}"
                                        BorderBrush="Black"
                                        DockPanel.Dock="Left"
                                        Height="15"
                                        Width="15"
                                        Margin="0,0,5,0"
                                        BorderThickness="1"
                                        CornerRadius="10" />
                                <TextBlock DockPanel.Dock="Right"
                                           Text="{Binding TemplateName}" />
                            </DockPanel>
                        </ItemContainerTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </local:MpClipBorder>
            <Button x:Name="ClearAllTemplatesButton"
                    Grid.Column="1"
                    TabIndex="0"
                    Visibility="{Binding IsAllTemplatesFilled, Converter={StaticResource BoolToVisibilityFlipConverter}}"
                    Command="{Binding ClearAllTemplatesCommand}"
                    Margin="3,0,3,0"
                    Padding="0,0,0,0"
                    FontSize="12"
                    VerticalContentAlignment="Top"
                    VerticalAlignment="Center"
                    Content="CLEAR ALL" />
            <local:MpClipBorder Grid.Column="2"
                                CornerRadius="10"
                                Background="White"
                                BorderThickness="0.5"
                                BorderBrush="DimGray"
                                Margin="1"
                                Padding="0"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Stretch">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="0.1*" />
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="SelectedTemplateTextBox"
                             Grid.Column="0"
                             TabIndex="1"
                             BorderThickness="0"
                             Text="{Binding SelectedTemplate.TemplateText, Mode=TwoWay, NotifyOnTargetUpdated=True, NotifyOnSourceUpdated=True, UpdateSourceTrigger=PropertyChanged}"
                             Foreground="Black"
                             FontSize="16"
                             Height="24"
                             Width="240"
                             Padding="0,0,0,0"
                             Margin="5,0,0,0"
                             VerticalAlignment="Top"
                             HorizontalAlignment="Left"
                             VerticalContentAlignment="Top" />
                    <Button x:Name="ClearCurrentTemplateButton"
                            Grid.Column="1"
                            Visibility="{Binding SelectedTemplate.HasText, Converter={StaticResource BoolToVisibilityConverter}}"
                            Command="{Binding SelectedTemplate.ClearTemplateCommand}"
                            Background="Transparent"
                            BorderThickness="0"
                            Width="15"
                            Height="15">
                        <Image x:Name="ClearCurrentTemplateButtonImage"
                               Source="/Resources/Images/close1.png" />
                    </Button>
                </Grid>
            </local:MpClipBorder>
            <StackPanel Orientation="Horizontal"
                        Grid.Column="3"
                        Visibility="{Binding HasMultipleTemplates,Converter={StaticResource BoolToVisibilityConverter}}"
                        HorizontalAlignment="Center"
                        Margin="8,0,0,0">
                <Button x:Name="PreviousTemplateButton"
                        TabIndex="2"
                        Command="{Binding SelectPreviousTemplateCommand}"
                        Width="20"
                        Height="20"
                        Margin="0"
                        Padding="5">
                    <Image Source="/Resources/Images/back_black.png" />
                </Button>
                <Button x:Name="NextTemplateButton"
                        TabIndex="3"
                        Command="{Binding SelectNextTemplateCommand}"
                        Width="20"
                        Height="20"
                        Margin="1"
                        Padding="5">
                    <Image Source="/Resources/Images/forward_black.png" />
                </Button>
            </StackPanel>
            <Button x:Name="PasteTemplateButton"
                    Grid.Column="4"
                    TabIndex="4"
                    Visibility="{Binding IsAllTemplatesFilled, Converter={StaticResource BoolToVisibilityConverter}}"
                    Command="{Binding PasteTemplateCommand}"
                    Content="{Binding PasteButtonText}"
                    Margin="15,0,15,0"
                    Padding="10,5,10,5"
                    FontSize="12"
                    HorizontalAlignment="Left"
                    VerticalContentAlignment="Top"
                    VerticalAlignment="Center" />
        </Grid>
    </Border>
</UserControl>
