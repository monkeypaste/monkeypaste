<local:MpUserControl x:Class="MpWpfApp.MpContentItemView"
                     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                     xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
                     xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                     xmlns:spinners="clr-namespace:xamlSpinnersWPF"
                     xmlns:local="clr-namespace:MpWpfApp"
                     x:TypeArguments="local:MpContentItemViewModel"
                     mc:Ignorable="d"
                     d:DesignHeight="100"
                     d:DesignWidth="800"
                     d:DataContext="{d:DesignInstance local:MpContentItemViewModel, IsDesignTimeCreatable=True}"
                     HorizontalAlignment="Stretch"
                     VerticalAlignment="Stretch">
    <i:Interaction.Behaviors>
        <local:MpContentDragBehavior x:Name="DragBehavior" />
    </i:Interaction.Behaviors>
    <Border BorderThickness="1"
            Loaded="ContentListItemView_Loaded"
            DataContextChanged="ContentListItemView_DataContextChanged"
            MouseEnter="ContentListItemView_MouseEnter"
            MouseLeave="ContentListItemView_MouseLeave">
        <Border.Resources>
            <DataTemplate x:Key="TextContent">
                <local:MpRtbView DataContext="{Binding}" />
            </DataTemplate>
            <DataTemplate x:Key="ImageContent">
                <local:MpImageItemView DataContext="{Binding}" />
            </DataTemplate>
            <DataTemplate x:Key="FileListContent">
                <local:MpFileListItemView DataContext="{Binding}" />
            </DataTemplate>
            <local:MpContentViewSelector x:Key="ContentViewSelector"
                                         TextContentTemplate="{StaticResource TextContent}"
                                         FileContentTemplate="{StaticResource FileListContent}"
                                         ImageContentTemplate="{StaticResource ImageContent}" />
        </Border.Resources>
        <Border.BorderBrush>
            <DrawingBrush Viewport="0,0,7,7"
                          ViewportUnits="Absolute"
                          TileMode="Tile">
                <DrawingBrush.Drawing>
                    <DrawingGroup>
                        <GeometryDrawing Brush="{Binding ItemBorderBrush}">
                            <GeometryDrawing.Geometry>
                                <GeometryGroup>
                                    <RectangleGeometry Rect="{Binding ItemBorderBrushRect}" />
                                    <RectangleGeometry Rect="50,50,50,50" />
                                </GeometryGroup>
                            </GeometryDrawing.Geometry>
                        </GeometryDrawing>
                    </DrawingGroup>
                </DrawingBrush.Drawing>
            </DrawingBrush>
        </Border.BorderBrush>
        <ContentControl x:Name="ContentView"
                        Content="{Binding}"
                        ContentTemplateSelector="{StaticResource ContentViewSelector}" />
    </Border>
</local:MpUserControl>
