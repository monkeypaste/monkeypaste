<UserControl x:Class="MpWpfApp.MpContentListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:spinners="clr-namespace:xamlSpinnersWPF"
             DataContextChanged="UserControl_DataContextChanged"
             xmlns:local="clr-namespace:MpWpfApp"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800"
             d:DataContext="{d:DesignInstance local:MpClipTileViewModel, IsDesignTimeCreatable=True}">
    <DockPanel x:Name="ContentListDockPanel" 
                   Background="{local:MpSettingBinding ThemeClipTileBackgroundColor, Converter={StaticResource StringToBrushConverter}}"
                   Tag="{Binding DataContext,RelativeSource={RelativeSource Mode=Self}}"
                   HorizontalAlignment="Stretch"                   
                   VerticalAlignment="Stretch">
        <local:MpRtbEditToolbarView x:Name="EditToolbarView"
                                        DockPanel.Dock="Top"
                                        DataContext="{Binding PrimaryItem}" />
        <local:MpEditTemplateToolbarView x:Name="EditTemplateView"
                                             DockPanel.Dock="Bottom"
                                             Visibility="{Binding IsAnyEditingTemplate, Converter={StaticResource BoolToVisibilityConverter}}"
                                             DataContext="{Binding PrimaryItem.TemplateCollection}" />
        <local:MpPasteTemplateToolbarView x:Name="PasteTemplateView"
                                              DockPanel.Dock="Bottom"
                                              Visibility="{Binding Parent.IsPastingTemplate, Converter={StaticResource BoolToVisibilityConverter}}"
                                              DataContext="{Binding PrimaryItem.TemplateCollection}" />

        <AdornerDecorator ClipToBounds="True">
            <local:MpMultiSelectListBox x:Name="ContentListBox"
                                        Loaded="ContentListBox_Loaded"
                                        RequestBringIntoView="ContentListBox_RequestBringIntoView"          
                                        SelectionChanged="ContentListBox_SelectionChanged"                                        
                                        ClipToBounds="True"
                                        VerticalAlignment="Stretch"
                                        HorizontalAlignment="Stretch"
                                        HorizontalContentAlignment="Stretch"
                                        VerticalContentAlignment="Stretch"
                                        BorderThickness="0"
                                        BorderBrush="Pink"
                                        SelectionMode="Extended"
                                        ScrollToSelectedItem="False"
                                        IsListBoxHorizontal="False"
                                        IsSynchronizedWithCurrentItem="False"
                                        ItemsSource="{Binding ItemViewModels}"
                                        Focusable="True"
                                        IsEnabled="True"                                        
                                        VirtualizingPanel.IsVirtualizing="True"
                                        VirtualizingPanel.ScrollUnit="Pixel"
                                        VirtualizingPanel.VirtualizationMode="Recycling"    
                                        ScrollViewer.CanContentScroll="False"
                                        ScrollViewer.VerticalScrollBarVisibility="Hidden"
                                        ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                                        ScrollViewer.ScrollChanged="ContentListBox_ScrollChanged"
                                        Padding="0">
            <i:Interaction.Behaviors>
                <local:MpDropBehavior x:Name="DropBehavior"/>
            </i:Interaction.Behaviors>
            <local:MpMultiSelectListBox.InputBindings>
                <KeyBinding Gesture="Ctrl+A"
                                Command="{Binding SelectAllCommand}" />
            </local:MpMultiSelectListBox.InputBindings>
            <local:MpMultiSelectListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel IsItemsHost="True"
                                            Orientation="Vertical" />
                </ItemsPanelTemplate>
            </local:MpMultiSelectListBox.ItemsPanel>
            <local:MpMultiSelectListBox.ItemContainerStyle>
                <Style TargetType="{x:Type ListBoxItem}">
                    <Setter Property="IsSelected"
                                Value="{Binding IsSelected, Mode=TwoWay, NotifyOnTargetUpdated=True, NotifyOnSourceUpdated=True, UpdateSourceTrigger=PropertyChanged}" />
                    <Setter Property="FocusVisualStyle"
                                Value="{x:Null}" />
                    <Setter Property="HorizontalContentAlignment"
                                Value="Left" />
                    <Setter Property="VerticalContentAlignment"
                                Value="Top" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                <ContentPresenter />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </local:MpMultiSelectListBox.ItemContainerStyle>
            <local:MpMultiSelectListBox.ItemTemplate>
                <DataTemplate>
                    <local:MpContentItemView DataContext="{Binding}" />
                </DataTemplate>
            </local:MpMultiSelectListBox.ItemTemplate>
            </local:MpMultiSelectListBox>
        </AdornerDecorator>
    </DockPanel>        
</UserControl>  
