<local:MpUserControl x:Class="MpWpfApp.MpContentListView"
                     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                     xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
                     xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                     xmlns:dd="clr-namespace:GongSolutions.Wpf.DragDrop;assembly=GongSolutions.Wpf.DragDrop"
                     xmlns:local="clr-namespace:MpWpfApp"
                     x:TypeArguments="local:MpClipTileViewModel"
                     mc:Ignorable="d"
                     d:DesignHeight="450"
                     d:DesignWidth="800"
                     d:DataContext="{d:DesignInstance Type=local:MpClipTileViewModel,
                                                      IsDesignTimeCreatable=False}">
    <local:MpUserControl.Resources>
        <Style x:Key="ItemContentControlStyle"
               TargetType="ContentControl">
            <Style.Resources>
                <SolidColorBrush Color="Transparent"
                                 x:Key="{x:Static SystemColors.HighlightBrushKey}" />
                <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}"
                                 Color="Transparent" />
                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}"
                                 Color="Transparent" />
                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}"
                                 Color="Transparent" />
            </Style.Resources>
            <Setter Property="FocusVisualStyle"
                    Value="{x:Null}" />
            <Setter Property="Background"
                    Value="Transparent" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContentControl">
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver"
                                     Value="True">
                                <Setter Property="BorderBrush"
                                        Value="Transparent" />
                                <Setter Property="Background"
                                        Value="Transparent" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                        <ContentPresenter />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <DataTemplate x:Key="RtbDataTemplate">
            <local:MpTextContentView DataContext="{Binding}" />
        </DataTemplate>
        <DataTemplate x:Key="ContentListDataTemplate">
            <ListBox x:Name="ContentListBox"
                     Loaded="ContentListBox_Loaded"
                     Unloaded="ContentListDockPanel_Unloaded"
                     SelectionChanged="ContentListBox_SelectionChanged"
                     PreviewMouseWheel="ContentListBox_PreviewMouseWheel"
                     MouseDown="ContentListBox_MouseDown"
                     ClipToBounds="True"
                     HorizontalAlignment="Stretch"
                     HorizontalContentAlignment="Left"
                     VerticalAlignment="Stretch"
                     VerticalContentAlignment="Stretch"
                     BorderThickness="0"
                     SelectionMode="Extended"
                     IsSynchronizedWithCurrentItem="False"
                     ItemsSource="{Binding ItemViewModels}"
                     IsEnabled="True"
                     VirtualizingPanel.IsVirtualizing="True"
                     VirtualizingPanel.ScrollUnit="Pixel"
                     VirtualizingPanel.VirtualizationMode="Recycling"
                     ScrollViewer.CanContentScroll="False"
                     ScrollViewer.VerticalScrollBarVisibility="{Binding VerticalScrollbarVisibility}"
                     ScrollViewer.HorizontalScrollBarVisibility="{Binding HorizontalScrollbarVisibility}"
                     ScrollViewer.ScrollChanged="ContentListBox_ScrollChanged"
                     Padding="0">
                <ListBox.InputBindings>
                    <KeyBinding Gesture="Ctrl+A"
                                Command="{Binding SelectAllCommand}" />
                </ListBox.InputBindings>
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel IsItemsHost="True"
                                                VerticalAlignment="Stretch"
                                                HorizontalAlignment="Stretch"
                                                Orientation="Vertical" />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="{x:Type ListBoxItem}">
                        <Setter Property="IsSelected"
                                Value="{Binding IsSelected,
                                                Mode=TwoWay,
                                                NotifyOnTargetUpdated=True,
                                                NotifyOnSourceUpdated=True,
                                                UpdateSourceTrigger=PropertyChanged}" />
                        <Setter Property="FocusVisualStyle"
                                Value="{x:Null}" />
                        <Setter Property="HorizontalContentAlignment"
                                Value="Stretch" />
                        <Setter Property="VerticalContentAlignment"
                                Value="Stretch" />
                        <Setter Property="Background"
                                Value="Transparent" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                    <ContentPresenter />
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <local:MpContentItemView DataContext="{Binding}" />
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </DataTemplate>
        <local:MpContentListViewSelector x:Key="ContentListViewSelector" />
    </local:MpUserControl.Resources>
    <i:Interaction.Behaviors>
        <!--<local:MpContentListDropBehavior IsDebugEnabled="False"
                                         x:Name="ContentListDropBehavior" />-->
    </i:Interaction.Behaviors>
    <DockPanel x:Name="ContentListDockPanel"
               DataContextChanged="UserControl_DataContextChanged"
               Unloaded="ContentListDockPanel_Unloaded"
               HorizontalAlignment="Stretch"
               VerticalAlignment="Stretch">
        <local:MpRtbEditToolbarView x:Name="EditToolbarView"
                                    DockPanel.Dock="Top"
                                    Visibility="{Binding IsContentReadOnly,
                                                         Converter={StaticResource BoolToVisibilityFlipConverter}}"
                                    DataContext="{Binding PrimaryItem}" />
        <local:MpEditTemplateToolbarView x:Name="EditTemplateView"
                                         DockPanel.Dock="Bottom"
                                         Visibility="{Binding IsAnyEditingTemplate,
                                                              Converter={StaticResource BoolToVisibilityConverter},
                                                              TargetNullValue=Collapsed}"
                                         DataContext="{Binding PrimaryItem.TemplateCollection}" />
        <local:MpPasteTemplateToolbarView x:Name="PasteTemplateView"
                                          DockPanel.Dock="Bottom"
                                          Visibility="{Binding Parent.IsPastingTemplate,
                                                               Converter={StaticResource BoolToVisibilityConverter},
                                                               TargetNullValue=Collapsed}"
                                          DataContext="{Binding PrimaryItem.TemplateCollection}" />
        <!--<local:MpRtbEditToolbarView x:Name="EditToolbarView"
                                    DockPanel.Dock="Top"
                                    Visibility="Collapsed"
                                    DataContext="{Binding PrimaryItem}" />
        <local:MpEditTemplateToolbarView x:Name="EditTemplateView"
                                         DockPanel.Dock="Bottom"
                                         Visibility="Collapsed"
                                         DataContext="{Binding PrimaryItem.TemplateCollection}" />
        <local:MpPasteTemplateToolbarView x:Name="PasteTemplateView"
                                          DockPanel.Dock="Bottom"
                                          Visibility="Collapsed"
                                          DataContext="{Binding PrimaryItem.TemplateCollection}" />-->
        <!--<AdornerDecorator>
            <ContentControl ContentTemplateSelector="{StaticResource ContentListViewSelector}"
                            Loaded="ContentControl_Loaded"
                            Content="{Binding}" />
        </AdornerDecorator>-->

        <local:MpTextContentView DataContext="{Binding}" />
    </DockPanel>
</local:MpUserControl>
