<local:MpUserControl x:Class="MpWpfApp.MpAnalyticItemCollectionView"
                     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                     xmlns:local="clr-namespace:MpWpfApp"
                     x:TypeArguments="local:MpAnalyticItemCollectionViewModel"
                     xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                     mc:Ignorable="d"
                     d:DesignHeight="450"
                     d:DesignWidth="800"
                     d:DataContext="{d:DesignInstance Type=local:MpAnalyticItemCollectionViewModel,
                                                      IsDesignTimeCreatable=False}">
    <Grid>
        <Grid.Resources>
            <ResourceDictionary Source="/Views/MainWindow/ClipTray/ClipTile/Container/Analytics/Styles/MpAnalyticTreeViewStyle.xaml"
                                x:Name="TreeStyle" />
        </Grid.Resources>

        <TreeView Name="AnalyticTreeView"
                  BorderThickness="0"
                  Background="Transparent"
                  ScrollViewer.HorizontalScrollBarVisibility="Hidden">
            <!--<TreeView.ItemContainerStyle>
                <Style TargetType="TreeViewItem">
                    <Setter Property="IsExpanded"
                            Value="{Binding IsExpanded,
                                            Mode=TwoWay}" />
                    <Setter Property="HorizontalContentAlignment"
                            Value="Left" />
                    <Setter Property="VerticalContentAlignment"
                            Value="Top" />
                </Style>
            </TreeView.ItemContainerStyle>-->
            <TreeViewItem ItemsSource="{Binding Items}">
                <TreeViewItem.Resources>
                    <HierarchicalDataTemplate DataType="{x:Type local:MpAnalyticItemViewModel}"
                                              ItemsSource="{Binding Children}">
                        <local:MpAnalyticItemView MinWidth="250" DataContext="{Binding}" />
                    </HierarchicalDataTemplate>
                    <HierarchicalDataTemplate DataType="{x:Type local:MpAnalyticItemComponentViewModel}">
                        <local:MpAnalyticItemComponentView DataContext="{Binding}" />
                    </HierarchicalDataTemplate>
                </TreeViewItem.Resources>
                <TreeViewItem.ItemContainerStyle>
                    <Style TargetType="{x:Type TreeViewItem}">
                        <Style.Resources>
                            <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}"
                                             Color="Transparent" />
                            <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}"
                                             Color="Transparent" />
                            <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}"
                                             Color="Transparent" />
                        </Style.Resources>
                        <Setter Property="IsExpanded"
                            Value="{Binding IsExpanded,
                                            Mode=TwoWay}" />
                        <Setter Property="HorizontalContentAlignment"
                            Value="Left" />
                        <Setter Property="VerticalContentAlignment"
                            Value="Top" />
                        <Setter Property="Background"
                                Value="Transparent"/>
                    </Style>
                </TreeViewItem.ItemContainerStyle>
                <TreeViewItem.Header>
                    <local:MpClipBorder x:Name="AnalyticRootItemBorder"
                                        MouseEnter="AnalyticRootItemBorder_MouseEnter"
                                        MouseLeave="AnalyticRootItemBorder_MouseLeave"
                                        CornerRadius="10"
                                        Focusable="False"
                                        VerticalAlignment="Stretch"
                                        HorizontalAlignment="Left"
                                        Background="Transparent"
                                        BorderThickness="0">
                        <TextBlock x:Name="TagTextBlock"
                                   Grid.Column="1"
                                   Grid.Row="0"
                                   Margin="3,0,0,0"
                                   Padding="5"
                                   Background="Transparent"
                                   HorizontalAlignment="Left"
                                   VerticalAlignment="Center"
                                   FontWeight="SemiBold"
                                   Foreground="White"
                                   Text="Analyzers"
                                   FontSize="18" />
                    </local:MpClipBorder>
                </TreeViewItem.Header>
            </TreeViewItem>
        </TreeView>
    </Grid>
</local:MpUserControl>
