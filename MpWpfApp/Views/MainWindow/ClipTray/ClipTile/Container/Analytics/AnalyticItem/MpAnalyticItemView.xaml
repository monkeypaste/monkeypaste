<local:MpUserControl x:Class="MpWpfApp.MpAnalyticItemView"
                     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                     xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                     xmlns:local="clr-namespace:MpWpfApp"
                     x:TypeArguments="local:MpAnalyticItemViewModel"
                     mc:Ignorable="d"
                     d:DesignHeight="450"
                     d:DesignWidth="800"
                     d:DataContext="{d:DesignInstance local:MpAnalyticItemViewModel, IsDesignTimeCreatable=True}">
    <Border Margin="5"
            BorderThickness="1"
            BorderBrush="Black"
            CornerRadius="3"
            Background="{Binding ItemBackgroundBrush}"
            MouseEnter="Border_MouseEnter"
            MouseLeave="Border_MouseLeave">
        <StackPanel Orientation="Horizontal">
            <Image Source="{Binding ItemIconSourcePath}"
                   Margin="5,0,5,0"
                   Height="25"
                   Width="25" />
            <TextBlock x:Name="AnalyticItemTextBlock"
                       Margin="8,0,0,0"
                       Padding="3"
                       MinWidth="170"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Left"
                       FontSize="16"
                       Foreground="Black"
                       Background="Transparent">
                <TextBlock.ToolTip>
                    <ToolTip>
                        <TextBlock Text="{Binding Description}" />
                    </ToolTip>
                </TextBlock.ToolTip>
                <TextBlock.Inlines>
                    <Hyperlink x:Name="AnalyticItemTextBlockLink"
                               Background="Transparent">
                        <Hyperlink.Inlines>
                            <Run Text="{Binding Title}" />
                        </Hyperlink.Inlines>
                    </Hyperlink>
                </TextBlock.Inlines>
            </TextBlock>
        </StackPanel>
    </Border>
</local:MpUserControl>
