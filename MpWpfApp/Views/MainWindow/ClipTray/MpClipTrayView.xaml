<local:MpUserControl x:Class="MpWpfApp.MpClipTrayView"
                     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                     xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
                     xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                     xmlns:spinners="clr-namespace:xamlSpinnersWPF"
                     xmlns:local="clr-namespace:MpWpfApp"
                     x:TypeArguments="local:MpClipTrayViewModel"
                     DataContext="{StaticResource ClipTrayViewModel}"
                     mc:Ignorable="d"
                     d:DesignHeight="450"
                     d:DesignWidth="800"
                     d:DataContext="{d:DesignInstance local:MpClipTrayViewModel, IsDesignTimeCreatable=True}">
    <local:MpUserControl.Resources>
        <local:MpAnalyticItemCollectionView x:Key="AnalyticItemCollectionView"/>
        <local:MpClipTileFlipView x:Key="ClipTileFlipView"/>
    </local:MpUserControl.Resources>
    <Grid x:Name="ClipTrayGrid">
        <AdornerDecorator>
            <ListBox x:Name="ClipTray"
                     RequestBringIntoView="ClipTray_RequestBringIntoView"
                     SelectionChanged="ClipTray_SelectionChanged"
                     DataContext="{Binding}"
                     Visibility="{Binding ClipTrayVisibility}"
                     Width="{Binding ClipTrayWidth, Source={StaticResource MainWindowViewModel}}"
                     ItemsSource="{Binding Items}"
                     ForceCursor="True"
                     Cursor="{Binding IsBusy, Converter={StaticResource BoolToCursorConverter}, ConverterParameter='wait,arrow'}"
                     local:MpLoadMoreItemsExtension.RemainingItemsThreshold="{Binding RemainingItemsCountThreshold}"
                     local:MpLoadMoreItemsExtension.RemainingItemsThresholdReachedCommand="{Binding LoadMoreClipsCommand}"
                     Loaded="ClipTray_Loaded"
                     IsSynchronizedWithCurrentItem="False"
                     Margin="0,0,0,0"
                     Focusable="True"
                     BorderThickness="0"
                     VerticalAlignment="Center"
                     VerticalContentAlignment="Top"
                     HorizontalAlignment="Left"
                     HorizontalContentAlignment="Center"
                     Background="Transparent"
                     SelectionMode="Single"
                     VirtualizingPanel.IsVirtualizing="True"
                     VirtualizingPanel.ScrollUnit="Pixel"
                     VirtualizingPanel.VirtualizationMode="Recycling"
                     ScrollViewer.CanContentScroll="False"
                     ScrollViewer.IsDeferredScrollingEnabled="True"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto"
                     ScrollViewer.VerticalScrollBarVisibility="Hidden">
                <i:Interaction.Behaviors>
                    <local:MpDropBehavior x:Name="DropBehavior" />
                </i:Interaction.Behaviors>
                <ListBox.InputBindings>
                    <KeyBinding Gesture="Ctrl+A"
                                Command="{Binding SelectAllCommand}" />
                </ListBox.InputBindings>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="{x:Type ListBoxItem}">
                        <Setter Property="IsSelected"
                                Value="{Binding IsSelected}" />
                        <Setter Property="FocusVisualStyle"
                                Value="{x:Null}" />
                        <Setter Property="HorizontalContentAlignment"
                                Value="Left" />
                        <Setter Property="VerticalContentAlignment"
                                Value="Top" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                    <ContentPresenter />
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected"
                                                 Value="True">
                                            <Setter Property="BorderBrush"
                                                    Value="Transparent" />
                                            <Setter Property="Background"
                                                    Value="Transparent" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel x:Name="ClipTrayVirtualizingStackPanel"
                                    CanVerticallyScroll="False"
                                    CanHorizontallyScroll="True"
                                    VerticalAlignment="Top"
                                    HorizontalAlignment="{Binding IsAnyTileExpanded, Source={StaticResource ClipTrayViewModel}, Converter={StaticResource BoolToHAlignmentConverter}, ConverterParameter='center,left'}"
                                    IsItemsHost="True"
                                    Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <local:MpClipTileContainerView Visibility="{Binding IsPlaceholder, Converter={StaticResource BoolToVisibilityFlipConverter}}"
                                                       DataContext="{Binding}" />
                        <!--<local:MpClipTileView Visibility="{Binding ItemVisibility}"
                                                  DataContext="{Binding}" />-->
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </AdornerDecorator>
        <TextBlock x:Name="EmptyListTextBlock"
                   Text="This list has no items"
                   Focusable="True"
                   Visibility="Collapsed"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Margin="0,0,0,15"
                   Foreground="LightGray"
                   FontStyle="Oblique"
                   FontSize="34" />
    </Grid>
</local:MpUserControl>
