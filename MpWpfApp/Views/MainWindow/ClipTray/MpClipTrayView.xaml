<local:MpUserControl x:Class="MpWpfApp.MpClipTrayView"
                     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                     xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
                     xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                     xmlns:local="clr-namespace:MpWpfApp"
                     x:TypeArguments="local:MpClipTrayViewModel"
                     mc:Ignorable="d"
                     d:DesignHeight="450"
                     d:DesignWidth="800"
                     d:DataContext="{d:DesignInstance Type=local:MpClipTrayViewModel,
                                                      IsDesignTimeCreatable=False}">
    <Grid x:Name="ClipTrayGrid">
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0"
                                 Opacity="0.0"
                                 EndPoint="0,1">
                <GradientStop Offset="0"
                              Color="LightGray" />
                <GradientStop Offset=".1"
                              Color="Transparent" />
            </LinearGradientBrush>
        </Grid.Background>
        <Grid>
            <AdornerDecorator>
                <ListBox x:Name="ClipTray"
                         RequestBringIntoView="ClipTray_RequestBringIntoView"
                         SelectionChanged="ClipTray_SelectionChanged"
                         Visibility="{Binding IsTrayEmpty,
                                              Converter={StaticResource BoolToVisibilityFlipConverter},
                                              ConverterParameter={StaticResource Hide}}"
                         ItemsSource="{Binding Items}"
                         Background="Transparent"
                         local:MpFrictionScrollExtension.IsEnabled="True"
                         local:MpFrictionScrollExtension.IsHorizontal="True"
                         local:MpFrictionScrollExtension.WheelDampening="0.08"
                         local:MpFrictionScrollExtension.Friction="0.85"
                         local:MpLoadMoreItemsExtension.RemainingItemsThreshold="{Binding RemainingItemsCountThreshold}"
                         local:MpLoadMoreItemsExtension.RemainingItemsThresholdReachedCommand="{Binding LoadMoreClipsCommand}"
                         Loaded="ClipTray_Loaded"
                         IsSynchronizedWithCurrentItem="False"
                         Margin="0,0,0,0"
                         Focusable="True"
                         BorderThickness="0"
                         VerticalAlignment="Stretch"
                         VerticalContentAlignment="Stretch"
                         HorizontalAlignment="Stretch"
                         HorizontalContentAlignment="Stretch"
                         SelectionMode="Single"
                         VirtualizingPanel.IsVirtualizing="True"
                         VirtualizingPanel.ScrollUnit="Pixel"
                         VirtualizingPanel.VirtualizationMode="Recycling"
                         ScrollViewer.CanContentScroll="False"
                         ScrollViewer.IsDeferredScrollingEnabled="True"
                         ScrollViewer.HorizontalScrollBarVisibility="Auto"
                         ScrollViewer.VerticalScrollBarVisibility="Hidden">
                    <i:Interaction.Behaviors>
                        <local:MpDropBehavior x:Name="DropBehavior" />
                    </i:Interaction.Behaviors>
                    <ListBox.InputBindings>
                        <KeyBinding Gesture="Ctrl+A"
                                    Command="{Binding SelectAllCommand}" />
                    </ListBox.InputBindings>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="{x:Type ListBoxItem}">
                            <Setter Property="IsSelected"
                                    Value="{Binding IsSelected}" />
                            <Setter Property="FocusVisualStyle"
                                    Value="{x:Null}" />
                            <Setter Property="HorizontalContentAlignment"
                                    Value="Stretch" />
                            <Setter Property="VerticalContentAlignment"
                                    Value="Top" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                        <ContentPresenter />
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected"
                                                     Value="True">
                                                <Setter Property="BorderBrush"
                                                        Value="Transparent" />
                                                <Setter Property="Background"
                                                        Value="Transparent" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <VirtualizingStackPanel x:Name="ClipTrayVirtualizingStackPanel"
                                                    HorizontalAlignment="Stretch"
                                                    IsItemsHost="True"
                                                    Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <local:MpClipTileContainerView Visibility="{Binding IsPlaceholder,
                                                                                Converter={StaticResource BoolToVisibilityFlipConverter}}"
                                                           DataContext="{Binding .,
                                                                                 IsAsync=True}" />
                            <!--<local:MpClipTileView Visibility="{Binding ItemVisibility}"
                                                  DataContext="{Binding}" />-->
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </AdornerDecorator>
            <TextBlock x:Name="EmptyListTextBlock"
                       Text="This list has no items"
                       Focusable="True"
                       Visibility="{Binding IsTrayEmpty,
                                            Converter={StaticResource BoolToVisibilityConverter}}"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Margin="0,0,0,15"
                       Foreground="LightGray"
                       FontStyle="Oblique"
                       FontSize="34" />
        </Grid>
        <Viewbox Visibility="{Binding IsRequery, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter={StaticResource Hide}}">
            <local:MpSpinnerApple Margin="20"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center" />
        </Viewbox>
    </Grid>
</local:MpUserControl>
