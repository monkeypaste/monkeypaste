<UserControl x:Class="MpWpfApp.MpClipTrayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:spinners="clr-namespace:xamlSpinnersWPF"
             xmlns:local="clr-namespace:MpWpfApp"
             DataContext="{StaticResource ClipTrayViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid x:Name="ClipTrayGrid">
        <AdornerDecorator>            
            <local:MpMultiSelectListBox x:Name="ClipTray"
                                        DataContext="{Binding}"
                                        ItemsSource="{Binding ClipTileViewModels}"
                                        Visibility="{Binding ClipTrayVisibility}"
                                        Width="{Binding MainWindowViewModel.ClipTrayWidth}"
                                        Height="{Binding MainWindowViewModel.ClipTrayHeight}"
                                        Loaded="ClipTray_Loaded" 
                                        DataContextChanged="ClipTray_DataContextChanged"
                                        DragLeave="ClipTray_DragLeave"
                                        DragOver="ClipTray_DragOver"
                                        Drop="ClipTray_Drop"
                                        SelectionChanged="ClipTray_SelectionChanged"
                                        MouseLeftButtonDown="ClipTray_MouseLeftButtonDown"
                                        IsListBoxHorizontal="True"
                                        AllowDrop="True"
                                        ScrollToSelectedItem="False"
                                        IsSynchronizedWithCurrentItem="True"
                                        Margin="0,0,0,0"
                                        Focusable="True"
                                        BorderThickness="0"
                                        VerticalAlignment="Stretch"
                                        Background="Transparent"
                                        SelectionMode="Extended"
                                        VirtualizingPanel.IsVirtualizing="True"
                                        VirtualizingPanel.ScrollUnit="Pixel"
                                        VirtualizingPanel.VirtualizationMode="Recycling"         
                                        ScrollViewer.ScrollChanged="ClipTray_ScrollChanged"
                                        ScrollViewer.CanContentScroll="False"
                                        ScrollViewer.IsDeferredScrollingEnabled="True"
                                        ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                        ScrollViewer.VerticalScrollBarVisibility="Hidden">
                <local:MpMultiSelectListBox.InputBindings>
                    <KeyBinding Gesture="Ctrl+A"
                                Command="{Binding SelectAllCommand}" />
                </local:MpMultiSelectListBox.InputBindings>
                <local:MpMultiSelectListBox.ItemContainerStyle>
                    <Style TargetType="{x:Type ListBoxItem}">
                        <Setter Property="IsSelected"
                                Value="{Binding IsSelected}" />
                        <Setter Property="FocusVisualStyle"
                                Value="{x:Null}" />
                        <Setter Property="HorizontalContentAlignment"
                                Value="Left" />
                        <Setter Property="VerticalContentAlignment"
                                Value="Top" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                    <ContentPresenter />
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected"
                                                 Value="True">
                                            <Setter Property="BorderBrush"
                                                    Value="Transparent" />
                                            <Setter Property="Background"
                                                    Value="Transparent" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </local:MpMultiSelectListBox.ItemContainerStyle>
                <local:MpMultiSelectListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel x:Name="ClipTrayVirtualizingStackPanel"
                                                Loaded="ClipTrayVirtualizingStackPanel_Loaded"
                                                IsItemsHost="True"
                                                CanVerticallyScroll="False"
                                                CanHorizontallyScroll="True"
                                                VerticalAlignment="Top"
                                                HorizontalAlignment="Left"
                                                Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </local:MpMultiSelectListBox.ItemsPanel>
                <local:MpMultiSelectListBox.ItemTemplate>
                    <DataTemplate>
                        <local:MpClipTileView DataContext="{Binding}" />
                    </DataTemplate>
                </local:MpMultiSelectListBox.ItemTemplate>
            </local:MpMultiSelectListBox>
        </AdornerDecorator>
        <TextBlock x:Name="EmptyListTextBlock"
                   Text="This list has no items"
                   Focusable="True"
                   Visibility="Collapsed"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Margin="0,0,0,15"
                   Foreground="LightGray"
                   FontStyle="Oblique"
                   FontSize="34" />
    </Grid>
</UserControl>
