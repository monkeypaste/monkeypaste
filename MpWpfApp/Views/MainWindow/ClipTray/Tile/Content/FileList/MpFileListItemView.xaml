<UserControl x:Class="MpWpfApp.MpFileListItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:spinners="clr-namespace:xamlSpinnersWPF"
             xmlns:local="clr-namespace:MpWpfApp"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <Border x:Name="FileListItem"
            Height="22"
            BorderThickness="0">
        <Grid>
            <StackPanel Visibility="{Binding HostClipTileViewModel.FileListStackPanelVisibility}"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Left"
                        ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                        ScrollViewer.VerticalScrollBarVisibility="Hidden"
                        Orientation="Horizontal">
                <Image Source="{Binding Icon, Converter={StaticResource ImageSourceToBitmapSourceConverter}}"
                       Height="22"
                       Stretch="Fill" />
                <TextBlock x:Name="FileListItemTextBlock"
                           Margin="3,0,0,0"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Left"
                           ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                           ScrollViewer.VerticalScrollBarVisibility="Hidden"
                           FontSize="16"
                           Foreground="Black"
                           Background="Transparent">
                        <TextBlock.ToolTip>
                            <ToolTip>
                                <TextBlock Text="{Binding ItemPath}" />
                            </ToolTip>
                        </TextBlock.ToolTip>
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="Loaded">
                                <i:CallMethodAction TargetObject="{Binding}"
                                                    MethodName="FileListItemTextBlock_Loaded" />
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                </TextBlock>
            </StackPanel>
            <WebBrowser x:Name="FileItemMoonPdfPanel"
                        Visibility="{Binding HostClipTileViewModel.WebBrowserFileViewerVisibility}"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center"
                        ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                        ScrollViewer.VerticalScrollBarVisibility="Hidden">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Loaded">
                        <i:CallMethodAction TargetObject="{Binding}"
                                            MethodName="FileItemWebBrowser_Loaded" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </WebBrowser>
        </Grid>
    </Border>
</UserControl>
