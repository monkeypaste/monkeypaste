<UserControl x:Class="MpWpfApp.MpClipTileContainerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MpWpfApp"
        mc:Ignorable="d" >
    <Grid>
        <local:MpClipTileView x:Name="ClipTileView" 
                              Visibility="{Binding IsFlipped, Converter={StaticResource BoolToVisibilityFlipConverter}, ConverterParameter={StaticResource Hide}}" />
        <local:MpClipTileFlipView Owner="{Binding ElementName=ClipTileView}"
                                  Visibility="{Binding IsFlipped, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter={StaticResource Hide}}" />
    </Grid>
</UserControl>
