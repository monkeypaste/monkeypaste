<local:MpUserControl x:Class="MpWpfApp.MpMatcherCollectionView"
                     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
                     xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                     xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                     xmlns:dd="clr-namespace:GongSolutions.Wpf.DragDrop;assembly=GongSolutions.Wpf.DragDrop"
                     xmlns:local="clr-namespace:MpWpfApp"
                     x:TypeArguments="local:MpAnalyticItemPresetViewModel"
                     mc:Ignorable="d"
                     d:DataContext="{d:DesignInstance Type=local:MpAnalyticItemPresetViewModel,
                                                      IsDesignTimeCreatable=False}"
                     d:DesignHeight="450"
                     d:DesignWidth="800">
    <Border IsVisibleChanged="Border_IsVisibleChanged"
            CornerRadius="3"
            Margin="10,0"
            BorderThickness="1"
            BorderBrush="Black"
            Background="Transparent">
        <StackPanel Orientation="Vertical">
            <TextBlock Text="Matchers"
                       FontSize="20"
                       Margin="5,0"
                       Foreground="White"
                       Background="Transparent"
                       FontWeight="SemiBold" />
            <DataGrid x:Name="AnalyticItemDatagrid"
                      dd:DragDrop.IsDragSource="True"
                      dd:DragDrop.IsDropTarget="True"
                      ItemsSource="{Binding PresetMatchers}"
                      SelectionMode="Single"
                      BorderThickness="10"
                      RowHeaderWidth="0"
                      Margin="5"
                      HorizontalAlignment="Stretch"
                      CanUserAddRows="False"
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      IsReadOnly="False"
                      AutoGenerateColumns="False">
                <DataGrid.Resources>
                    <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}"
                                     Color="Transparent" />
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTemplateColumn Width="100"
                                            Header="Trigger">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ComboBox IsSynchronizedWithCurrentItem="True"
                                          VerticalContentAlignment="Center"
                                          SelectedIndex="{Binding SelectedTriggerTypeIdx,
                                                                  Mode=TwoWay,
                                                                  UpdateSourceTrigger=PropertyChanged}"
                                          ItemsSource="{Binding TriggerTypes}">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding}" />
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Width="100"
                                            Header="Trigger Action">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ComboBox IsSynchronizedWithCurrentItem="True"
                                          VerticalContentAlignment="Center"
                                          SelectedIndex="{Binding SelectedTriggerActionTypeIdx,
                                                                  Mode=TwoWay,
                                                                  UpdateSourceTrigger=PropertyChanged}"
                                          ItemsSource="{Binding TriggerActionTypes}">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding}" />
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Width="100"
                                            Header="Match Text">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBox Margin="3"
                                         FontSize="16"
                                         Padding="5"
                                         Background="White"
                                         BorderThickness="0"
                                         Text="{Binding MatchData,
                                                        Mode=TwoWay,
                                                        NotifyOnSourceUpdated=True,
                                                        NotifyOnTargetUpdated=True,
                                                        UpdateSourceTrigger=PropertyChanged}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Width="100"
                                            Header="Match Action">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ComboBox IsSynchronizedWithCurrentItem="True"
                                          VerticalContentAlignment="Center"
                                          SelectedIndex="{Binding SelectedMatchActionTypeIdx,
                                                                  Mode=TwoWay,
                                                                  UpdateSourceTrigger=PropertyChanged}"
                                          ItemsSource="{Binding MatchActionTypes}">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding}" />
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn MinWidth="40">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Background="Transparent"
                                        BorderThickness="0"
                                        ToolTip="Delete"
                                        local:MpHoverableExtension.IsEnabled="True"
                                        local:MpHoverableExtension.HoverCursor="{Binding DeleteCursor}"
                                        Command="{Binding Parent.DeleteMatcherCommand}"
                                        CommandParameter="{Binding}">
                                    <Image Width="20"
                                           Height="20"
                                           Source="{Binding .,
                                                            Converter={StaticResource StringToImageSourceConverter},
                                                            Source={StaticResource TrashCanIcon}}" />
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
            <Button Width="150"
                    BorderThickness="0"
                    Background="Beige"
                    Style="{StaticResource RoundedButton}"
                    Margin="10,5,10,10"
                    HorizontalAlignment="Right"
                    Command="{Binding AddMatcherCommand,
                                      Source={StaticResource MatcherCollectionViewModel}}"
                    CommandParameter="{Binding}">
                <DockPanel>
                    <Image Source="{Binding .,
                                            Converter={StaticResource StringToImageSourceConverter},
                                            Source={StaticResource AddIcon}}"
                           DockPanel.Dock="Left"
                           Width="15"
                           Height="15" />
                    <Label DockPanel.Dock="Right"
                           Content="Add Matcher" />
                </DockPanel>
            </Button>
        </StackPanel>
    </Border>
</local:MpUserControl>
