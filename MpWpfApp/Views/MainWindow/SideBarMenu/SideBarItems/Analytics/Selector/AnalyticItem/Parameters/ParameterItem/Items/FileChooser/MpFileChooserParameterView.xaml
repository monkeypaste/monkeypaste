<local:MpUserControl x:Class="MpWpfApp.MpFileChooserParameterView"
                     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                     xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                     xmlns:local="clr-namespace:MpWpfApp"
                     xmlns:pcl="clr-namespace:MonkeyPaste;assembly=MonkeyPaste"
                     x:TypeArguments="local:MpFileChooserParameterViewModel"
                     mc:Ignorable="d"
                     d:DesignHeight="450"
                     d:DesignWidth="800"
                     d:DataContext="{d:DesignInstance Type=local:MpFileChooserParameterViewModel,
                                                      IsDesignTimeCreatable=False}">
    <Border MaxWidth="150"
            MinWidth="100"
            HorizontalAlignment="Center"
            Padding="10,3"
            ToolTip="{Binding CurrentValue}"
            Style="{StaticResource ActionPropertyComboBoxStyle_Border}">
        <Button Command="{Binding SelectFileSystemPathCommand}"
                Background="Transparent"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Stretch"
                BorderThickness="0"
                Style="{StaticResource SemiTransparentButton}">
            <StackPanel>
                <TextBlock Text="Select.."
                           Visibility="{Binding CurrentValue,
                                                Converter={StaticResource IsNotNullToVisibilityFlipConverter}}" />
                <TextBlock local:MpHoverableExtension.IsEnabled="True"
                           local:MpHoverableExtension.HoverCursor="Hand"
                           Visibility="{Binding CurrentValue,
                                                Converter={StaticResource IsNotNullToVisibilityConverter}}"
                           Text="{Binding CurrentValue,
                                          Converter={StaticResource AbsoluteToRelativePathStringConverter}}"
                           TextAlignment="Right"
                           VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Foreground" Value="Crimson" />
                                    <Setter Property="FontWeight" Value="Normal" />
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="False">
                                    <Setter Property="Foreground" Value="Black" />
                                    <Setter Property="FontWeight" Value="Normal" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>
        </Button>
    </Border>
</local:MpUserControl>
