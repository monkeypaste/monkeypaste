<local:MpUserControl x:Class="MpWpfApp.MpContentAddTriggerPropertyListBoxItemView"
                     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                     xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                     xmlns:local="clr-namespace:MpWpfApp"
                     xmlns:pcl="clr-namespace:MonkeyPaste;assembly=MonkeyPaste"
                     x:TypeArguments="local:MpContentAddTriggerViewModel"
                     mc:Ignorable="d"
                     d:DesignHeight="450"
                     d:DesignWidth="800"
                     d:DataContext="{d:DesignInstance Type=local:MpContentAddTriggerViewModel,
                                                      IsDesignTimeCreatable=False}">
    <Grid VerticalAlignment="Stretch"
          Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="0.5*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.2*" />
            <ColumnDefinition Width="0.6*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0"
                   Grid.Column="0"
                   Style="{StaticResource ActionPropertyTextBlockStyle}"
                   Text="Trigger" />
        <Border Grid.Row="0"
                Grid.Column="1"
                Style="{StaticResource ActionPropertyComboBoxStyle_Border}">
            <ComboBox Style="{StaticResource ActionPropertyComboBoxStyle}"
                      IsDropDownOpen="{Binding IsAnyDropDownOpen,
                                               Source={x:Static local:MpMainWindowViewModel.Instance},
                                               Mode=OneWayToSource}"
                      ItemContainerStyle="{StaticResource ActionPropertyComboBoxItemStyle}"
                      SelectedIndex="{Binding AddedContentType,
                                              Converter={StaticResource EnumToIntConverter},
                                              ConverterParameter=MonkeyPaste.MpCopyItemType,
                                              Mode=TwoWay}"
                      ItemsSource="{Binding .,
                                            Converter={StaticResource EnumToItemSourceLabelsConverter},
                                            ConverterParameter=MonkeyPaste.MpCopyItemType|All}" />
        </Border>
    </Grid>
</local:MpUserControl>
