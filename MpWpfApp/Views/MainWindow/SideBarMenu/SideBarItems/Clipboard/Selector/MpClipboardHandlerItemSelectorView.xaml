<local:MpUserControl x:Class="MpWpfApp.MpClipboardHandlerItemSelectorView"
                     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                     xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                     xmlns:dd="clr-namespace:GongSolutions.Wpf.DragDrop;assembly=GongSolutions.Wpf.DragDrop"
                     xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
                     xmlns:local="clr-namespace:MpWpfApp"
                     x:TypeArguments="local:MpClipboardHandlerCollectionViewModel"
                     mc:Ignorable="d"
                     d:DesignHeight="450"
                     d:DesignWidth="800"
                     d:DataContext="{d:DesignInstance Type=local:MpClipboardHandlerCollectionViewModel,
                                                      IsDesignTimeCreatable=False}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Border Grid.Row="0"
                Grid.Column="0"
                BorderThickness="0"
                BorderBrush="Black"
                Background="Transparent"
                VerticalAlignment="Top"
                Margin="10,0">
            <ComboBox x:Name="AnalyticItemChooserComboBox"
                      MinWidth="150"
                      IsSynchronizedWithCurrentItem="True"
                      IsDropDownOpen="{Binding IsAnyDropDownOpen,
                                               Source={x:Static local:MpMainWindowViewModel.Instance},
                                               Mode=OneWayToSource}"
                      SelectedItem="{Binding SelectedItem}"
                      ItemsSource="{Binding Items}">
                <ComboBox.ItemContainerStyle>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="IsSelected"
                                Value="{Binding IsSelected,
                                                Mode=TwoWay,
                                                UpdateSourceTrigger=PropertyChanged,
                                                NotifyOnTargetUpdated=True,
                                                NotifyOnSourceUpdated=True}" />
                    </Style>
                </ComboBox.ItemContainerStyle>
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <local:MpClipboardHandlerItemView DataContext="{Binding}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </Border>
        <local:MpHandledClipboardFormatsItemPresetDataGridView Grid.Row="1"
                                                               Grid.Column="0"
                                                               Margin="0,5,0,0"
                                                               VerticalAlignment="Top"
                                                               DataContext="{Binding SelectedItem}" />
        <local:MpClipboardHandlerItemPresetParameterListBoxView x:Name="ClipboardFormatPresetParameterListBoxView"
                                                                Grid.Column="1"
                                                                Grid.Row="0"
                                                                Grid.RowSpan="2"
                                                                HorizontalAlignment="Stretch"
                                                                DataContext="{Binding SelectedPresetViewModel}" />
    </Grid>
</local:MpUserControl>
