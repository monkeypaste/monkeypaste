<local:MpUserControl x:Class="MpWpfApp.MpTriggerActionChooserView"
                     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                     xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                     xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
                     xmlns:local="clr-namespace:MpWpfApp"
                     x:TypeArguments="local:MpActionCollectionViewModel"
                     mc:Ignorable="d"
                     d:DesignHeight="450"
                     d:DesignWidth="800"
                     d:DataContext="{d:DesignInstance Type=local:MpActionCollectionViewModel,
                                                      IsDesignTimeCreatable=False}">

    <StackPanel Orientation="Horizontal">
        <Border Grid.Column="0"
                CornerRadius="3"
                HorizontalAlignment="Stretch"
                BorderThickness="1"
                BorderBrush="Black"
                Margin="10">
            <i:Interaction.Behaviors>
                <local:MpResizeBehavior x:Name="ActionTreeResizeBehvior"
                                        IsResizing="{Binding IsResizing,
                                                             Mode=TwoWay,
                                                             UpdateSourceTrigger=PropertyChanged,
                                                             NotifyOnSourceUpdated=True,
                                                             NotifyOnTargetUpdated=True}"
                                        CanResize="{Binding CanResize,
                                                            Mode=TwoWay,
                                                            UpdateSourceTrigger=PropertyChanged,
                                                            NotifyOnSourceUpdated=True,
                                                            NotifyOnTargetUpdated=True}"
                                        ResizableEdges="Right"
                                        BoundWidth="{Binding SidebarWidth,
                                                             Mode=TwoWay,
                                                             UpdateSourceTrigger=PropertyChanged,
                                                             NotifyOnSourceUpdated=True,
                                                             NotifyOnTargetUpdated=True,
                                                             Source={x:Static local:MpSidebarViewModel.Instance}}"
                                        DefaultWidth="{Binding DefaultSidebarWidth}"
                                        MinWidth="{Binding DefaultSidebarWidth}" />

            </i:Interaction.Behaviors>
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="0.15*" />
                    <RowDefinition Height="0.2*" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0"
                           FontSize="18"
                           FontWeight="SemiBold"
                           Margin="0,0,0,5"
                           Foreground="White"
                           Text="Actions" />
                <DockPanel Grid.Row="1"
                           LastChildFill="True">
                    <Button BorderThickness="0"
                            DockPanel.Dock="Right"
                            Margin="5"
                            Background="Beige"
                            HorizontalAlignment="Right"
                            Command="{Binding ShowActionSelectorMenuCommand}"
                            CommandParameter="{Binding RelativeSource={RelativeSource Self}}">
                        <Image Source="{Binding .,
                                                Converter={StaticResource StringResourceToImageSourceConverter},
                                                Source={StaticResource AddIcon}}"
                               Margin="5"
                               Width="15"
                               Height="15" />
                    </Button>
                    <ComboBox x:Name="RootActionChooserComboBox"
                              DockPanel.Dock="Left"
                              MinWidth="200"
                              IsSynchronizedWithCurrentItem="True"
                              SelectedItem="{Binding SelectedItem,
                                                     Mode=TwoWay,
                                                     UpdateSourceTrigger=PropertyChanged}"
                              ItemsSource="{Binding Items}">
                        <ComboBox.ItemContainerStyle>
                            <Style TargetType="ComboBoxItem">
                                <Setter Property="IsSelected"
                                        Value="{Binding IsSelected,
                                                        Mode=TwoWay,
                                                        UpdateSourceTrigger=PropertyChanged,
                                                        NotifyOnTargetUpdated=True,
                                                        NotifyOnSourceUpdated=True}" />
                            </Style>
                        </ComboBox.ItemContainerStyle>
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <local:MpActionComboBoxItem Visibility="{Binding IsRootAction,
                                                                                 Converter={StaticResource BoolToVisibilityConverter}}"
                                                            DataContext="{Binding}" />
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </DockPanel>
                <local:MpTriggerActionTreeView Grid.Row="2"
                                               Margin="0,10,0,0"
                                               Visibility="{Binding IsAnySelected,
                                                                    Converter={StaticResource BoolToVisibilityConverter}}"
                                               DataContext="{Binding}" />
            </Grid>
        </Border>
        <Grid x:Name="DetailGrid"
              Grid.Column="1"
              MinWidth="500"
              Visibility="{Binding IsAnySelected,
                                   Converter={StaticResource BoolToVisibilityConverter},
                                   FallbackValue=Collapsed}">
            <i:Interaction.Behaviors>
                <local:MpResizeBehavior x:Name="SidebarResizeBehvior"
                                        DoubleClickControl="{Binding ElementName=SidebarGridSplitter}"
                                        IsResizing="{Binding IsResizing,
                                                             Mode=TwoWay,
                                                             UpdateSourceTrigger=PropertyChanged,
                                                             NotifyOnSourceUpdated=True,
                                                             NotifyOnTargetUpdated=True}"
                                        CanResize="{Binding CanResize,
                                                            Mode=TwoWay,
                                                            UpdateSourceTrigger=PropertyChanged,
                                                            NotifyOnSourceUpdated=True,
                                                            NotifyOnTargetUpdated=True}"
                                        ResizableEdges="Right"
                                        BoundWidth="{Binding SidebarWidth,
                                                             Mode=TwoWay,
                                                             UpdateSourceTrigger=PropertyChanged,
                                                             NotifyOnSourceUpdated=True,
                                                             NotifyOnTargetUpdated=True}"
                                        DefaultWidth="{Binding VisibleSidebar.DefaultWidth}"
                                        MinWidth="{Binding DefaultSidebarPanelWidth,
                                                           Source={x:Static local:MpMeasurements.Instance}}" />

            </i:Interaction.Behaviors>
            <local:MpActionDesignerView DataContext="{Binding SelectedItem}" />
        </Grid>
    </StackPanel>
</local:MpUserControl>
