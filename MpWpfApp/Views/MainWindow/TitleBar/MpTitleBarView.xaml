<local:MpUserControl x:Class="MpWpfApp.MpTitleBarView"
                     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
                     xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                     xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                     xmlns:local="clr-namespace:MpWpfApp"
                     x:TypeArguments="local:MpMainWindowViewModel"
                     mc:Ignorable="d"
                     d:DesignHeight="450"
                     d:DesignWidth="800"
                     d:DataContext="{d:DesignInstance Type=local:MpSearchBoxViewModel,
                                                      IsDesignTimeCreatable=False}">
    <i:Interaction.Behaviors>
        <local:MpMainWindowResizeBehavior x:Name="MainWindowResizeBehvior" />
        <local:MpExternalDropBehavior x:Name="ExternalDropBehavior" />
    </i:Interaction.Behaviors>
    <DockPanel LastChildFill="True">
        <TextBlock DockPanel.Dock="Left"
                   Text="{Binding LogText}" />
        <Button x:Name="FrameGripButton"
                Style="{StaticResource FrameGripButton}">
            <!--<Grid HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch" Background="Pink">
                <Grid.RowDefinitions>
                    <RowDefinition Height="0.25*" />
                    <RowDefinition Height="0.5*" />
                    <RowDefinition Height="0.25*" />
                </Grid.RowDefinitions>
                <Border x:Name="FrameGripTop" Background="Orange"
                        Grid.Row="0" />
                <Border x:Name="FrameGripMiddle"
                        Grid.Row="1">
                    <Border.Background>

                    </Border.Background>
                </Border>
                <Border x:Name="FrameGripBottom"
                        Grid.Row="2" />
            </Grid>-->
        </Button>
    </DockPanel>
</local:MpUserControl>
