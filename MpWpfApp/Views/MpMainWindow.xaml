<Window x:Class="MpWpfApp.MpMainWindow"
        x:Name="MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:s="clr-namespace:System;assembly=mscorlib"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
        xmlns:dd="clr-namespace:GongSolutions.Wpf.DragDrop;assembly=GongSolutions.Wpf.DragDrop"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        xmlns:local="clr-namespace:MpWpfApp"
        local:MpWindowClosingBehavior.Closed="{Binding ClosedCommand}"
        local:MpWindowClosingBehavior.Closing="{Binding ClosingCommand}"
        local:MpWindowClosingBehavior.CancelClosing="{Binding CancelClosingCommand}"
        Title="MpMainWindow"
        WindowStyle="None"
        AllowsTransparency="True"
        Top="10000"
        ShowInTaskbar="false"
        ResizeMode="NoResize"
        Background="Transparent"
        Height="350"
        Width="1680">
    <Window.DataContext>
        <local:MpMainWindowViewModel />
    </Window.DataContext>
    <Window.Resources>
        <s:Boolean x:Key="True">True</s:Boolean>
        <s:Boolean x:Key="False">False</s:Boolean>
        <!--<DataTemplate x:Key="ClipTileDragAdorner">
            <StackPanel>
                <Image Source="/MpWpfApp;component/Resources/asc_black.png"
                       Width="64"
                       HorizontalAlignment="Center" />
            </StackPanel>
        </DataTemplate>-->
        <local:MpSolidColorBrushToBrushConverter x:Key="SolidColorBrushToBrushConverter" />
        <!--<Style x:Key="SortOrderButtonStyle"
               TargetType="Button">
            <Setter Property="OverridesDefaultStyle"
                    Value="True" />
            <Setter Property="Margin"
                    Value="10,0,0,0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Name="border"
                                BorderThickness="0"
                                CornerRadius="15"
                                Width="30"
                                Height="30"
                                Background="Transparent">
                            <ContentPresenter HorizontalAlignment="Stretch"
                                              VerticalAlignment="Stretch" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver"
                                     Value="True">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <ImageBrush ImageSource="/MpWpfApp;component/Resources/asc_black.png" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsMouseOver"
                                     Value="False">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <ImageBrush ImageSource="/MpWpfApp;component/Resources/asc_white.png" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>-->
        <!--<Style x:Key="DropShaodowGridStyle"
               TargetType="{x:Type Grid}">
            <Style.Triggers>
                <Trigger Property="Visibility"
                         Value="Visible">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(Effect).BlurRadius"
                                                 From="0"
                                                 To="80"
                                                 Duration="0:0:0.2" />
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(Effect).BlurRadius"
                                                 From="80"
                                                 To="0"
                                                 Duration="0:0:0.2" />
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>-->
        <!--ScrollBars-->
        <Style x:Key="ScrollBarTrackThumb"
               TargetType="{x:Type Thumb}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Grid x:Name="Grid">
                            <Rectangle HorizontalAlignment="Stretch"
                                       VerticalAlignment="Stretch"
                                       Width="Auto"
                                       Height="Auto"
                                       Fill="Transparent" />
                            <Border x:Name="CornerScrollBarRectangle"
                                    CornerRadius="5"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Stretch"
                                    Width="Auto"
                                    Height="Auto"
                                    Margin="0,1,0,1"
                                    Background="{TemplateBinding Background}" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Tag"
                                     Value="Horizontal">
                                <Setter TargetName="CornerScrollBarRectangle"
                                        Property="Width"
                                        Value="Auto" />
                                <Setter TargetName="CornerScrollBarRectangle"
                                        Property="Height"
                                        Value="6" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="{x:Type ScrollBar}">
            <Setter Property="Stylus.IsFlicksEnabled"
                    Value="false" />
            <Setter Property="Foreground"
                    Value="#ADABAB" />
            <Setter Property="Background"
                    Value="Transparent" />
            <Setter Property="Width"
                    Value="7" />
            <Setter Property="VerticalAlignment"
                    Value="Bottom" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid x:Name="GridRoot"
                              Width="7"
                              Background="{TemplateBinding Background}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="0.00001*" />
                            </Grid.RowDefinitions>
                            <Track x:Name="PART_Track"
                                   Grid.Row="0"
                                   IsDirectionReversed="true"
                                   Focusable="false">
                                <Track.Thumb>
                                    <Thumb x:Name="Thumb"
                                           Background="{TemplateBinding Foreground}"
                                           Style="{DynamicResource ScrollBarTrackThumb}" />
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton x:Name="PageUp"
                                                  Command="ScrollBar.PageDownCommand"
                                                  Opacity="0"
                                                  Focusable="false" />
                                </Track.IncreaseRepeatButton>
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton x:Name="PageDown"
                                                  Command="ScrollBar.PageUpCommand"
                                                  Opacity="0"
                                                  Focusable="false" />
                                </Track.DecreaseRepeatButton>
                            </Track>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger SourceName="Thumb"
                                     Property="IsMouseOver"
                                     Value="true">
                                <Setter Value="{DynamicResource ButtonSelectBrush}"
                                        TargetName="Thumb"
                                        Property="Background" />
                            </Trigger>
                            <Trigger SourceName="Thumb"
                                     Property="IsDragging"
                                     Value="true">
                                <Setter Value="{DynamicResource DarkBrush}"
                                        TargetName="Thumb"
                                        Property="Background" />
                            </Trigger>

                            <Trigger Property="IsEnabled"
                                     Value="false">
                                <Setter TargetName="Thumb"
                                        Property="Visibility"
                                        Value="Collapsed" />
                            </Trigger>
                            <Trigger Property="Orientation"
                                     Value="Horizontal">
                                <Setter TargetName="GridRoot"
                                        Property="LayoutTransform">
                                    <Setter.Value>
                                        <RotateTransform Angle="-90" />
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="PART_Track"
                                        Property="LayoutTransform">
                                    <Setter.Value>
                                        <RotateTransform Angle="-90" />
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="Width"
                                        Value="Auto" />
                                <Setter Property="Height"
                                        Value="8" />
                                <Setter TargetName="Thumb"
                                        Property="Tag"
                                        Value="Horizontal" />
                                <Setter TargetName="PageDown"
                                        Property="Command"
                                        Value="ScrollBar.PageLeftCommand" />
                                <Setter TargetName="PageUp"
                                        Property="Command"
                                        Value="ScrollBar.PageRightCommand" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <SolidColorBrush x:Key="AppColorDark">DarkViolet</SolidColorBrush>
        <SolidColorBrush x:Key="AppColorLight">MediumPurple</SolidColorBrush>
    </Window.Resources>
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:CallMethodAction MethodName="MainWindow_Loaded"
                                TargetObject="{Binding}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <!-- Main Window Grid -->
    <Grid x:Name="MainWindowGrid"
          Background="Green">
        <Grid.RowDefinitions>
            <RowDefinition Height="{Binding TitleMenuHeight}" />
            <RowDefinition Height="{Binding FilterMenuHeight}"/>
            <RowDefinition Height="{Binding ClipTrayHeight}"/>
        </Grid.RowDefinitions>
        <tb:TaskbarIcon x:Name="TaskbarIcon"
                        IconSource="/MpWpfApp;component/Resources/monkey (2).ico"
                        ToolTipText="{Binding TaskbarIconToolTipText}">
            <tb:TaskbarIcon.TrayPopup>
                <Border Background="{StaticResource AppColorDark}"
                        BorderBrush="{StaticResource AppColorLight}"
                        BorderThickness="2"
                        CornerRadius="4"
                        Width="160"
                        Height="40">
                    <Button Content="Click Me!"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center" />
                </Border>
            </tb:TaskbarIcon.TrayPopup>
            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu Background="LightCoral">
                    <MenuItem Header="First Menu Item" />
                    <MenuItem Header="Second Menu Item" />
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
            <tb:TaskbarIcon.TrayToolTip>
                <Border Background="{StaticResource AppColorDark}"
                        BorderBrush="{StaticResource AppColorLight}"
                        BorderThickness="2"
                        Padding="10"
                        CornerRadius="4"
                        Opacity="0.8">
                    <StackPanel>
                        <TextBlock Text="Monkey Paste Info 1"
                                   Foreground="White"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center" />
                        <TextBlock Text="Monkey Paste Info 2"
                                   Foreground="White"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center" />
                        <TextBlock Text="Monkey Paste Info 3"
                                   Foreground="White"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center" />
                    </StackPanel>
                </Border>
            </tb:TaskbarIcon.TrayToolTip>
        </tb:TaskbarIcon>
        <!-- Main Menu -->
        <StackPanel Grid.Row="0"
                    Grid.Column="0"
                    Orientation="Horizontal"
                    Background="DarkGreen" />
        <!-- FilterMenu -->
        <Grid Grid.Row="1"
              Grid.Column="0"
              Focusable="True"
              Background="DarkViolet">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="7*" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>
            <!-- Sort Combobox -->
            <Grid Grid.Row="0"
                  Grid.Column="0"
                  Margin="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="4*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Label Grid.Column="0"
                       FontSize="19"
                       Foreground="White"
                       HorizontalAlignment="Right"
                       FontWeight="DemiBold"
                       VerticalContentAlignment="Center"
                       Padding="0,0,15,0">Sort By</Label>
                <local:MpClipBorder CornerRadius="15" 
                                    Margin="5"
                                    Grid.Column="1">
                    <ComboBox FontSize="16"
                              Background="Transparent"
                              HorizontalContentAlignment="Left"
                              VerticalContentAlignment="Center"
                              SelectedItem="{Binding SelectedSortType}"
                              ItemsSource="{Binding SortTypes}">
                        <ComboBox.ItemTemplate>
                            <ItemContainerTemplate>
                                <TextBlock Text="{Binding Header}" />
                            </ItemContainerTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </local:MpClipBorder>
                <Button Grid.Column="2"
                        Margin="0,0,0,0" 
                        Background="Transparent"
                        BorderThickness="0"
                        Height="30"
                        Width="30"
                        Command="{Binding ToggleSortOrderCommand}">
                    <Grid>
                        <Image Source="/MpWpfApp;component/Resources/asc_white.png"
                               Visibility="{Binding AscSortOrderButtonImageVisibility}"
                               Stretch="Fill" />
                        <Image Source="/MpWpfApp;component/Resources/desc_white.png"
                               Visibility="{Binding DescSortOrderButtonImageVisibility}"
                               Stretch="Fill" />
                    </Grid>
                    
                </Button>
            </Grid>
            <StackPanel Grid.Row="0"
                        Grid.Column="1"
                        Focusable="False"
                        HorizontalAlignment="Center"
                        Orientation="Horizontal">
                <ListBox x:Name="TagTray"
                         Focusable="False"
                         Background="Transparent"
                         BorderBrush="Transparent"
                         dd:DragDrop.IsDragSource="True"
                         dd:DragDrop.IsDropTarget="True"
                         HorizontalAlignment="Center"
                         SelectionMode="Single"
                         ScrollViewer.VerticalScrollBarVisibility="Hidden"
                         ItemsSource="{Binding TagTiles}">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="{x:Type ListBoxItem}">
                            <Setter Property="IsSelected"
                                    Value="{Binding IsSelected}" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                        <ContentPresenter />
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <VirtualizingStackPanel IsItemsHost="True"
                                                    ScrollViewer.VerticalScrollBarVisibility="Hidden"
                                                    Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <!-- Tag Tile Border -->
                            <Border CornerRadius="10"
                                    Margin="5"
                                    Padding="5"
                                    Focusable="True"
                                    VerticalAlignment="Center"
                                    local:MpFocusExtension.IsFocused="{Binding IsFocused, Mode=TwoWay, NotifyOnSourceUpdated=True,UpdateSourceTrigger=PropertyChanged}"
                                    Background="{Binding TagBorderBackgroundBrush}"
                                    BorderBrush="Transparent"
                                    BorderThickness="0"
                                    Height="{Binding TagHeight}">
                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="Loaded">
                                        <i:CallMethodAction TargetObject="{Binding}"
                                                            MethodName="TagTile_Loaded" />
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>
                                <Border.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem Header="Rename"
                                                  Command="{Binding RenameTagCommand}" />
                                        <MenuItem Header="Delete"
                                                  Command="{Binding MainWindowViewModel.DeleteTagCommand}" />
                                    </ContextMenu>
                                </Border.ContextMenu>
                                <StackPanel Orientation="Horizontal">
                                    <!-- Tag Count Box-->
                                    <local:MpClipBorder CornerRadius="3"
                                                        Padding="5"
                                                        BorderThickness="0"
                                                        Background="{Binding TagColor}">
                                        <TextBlock Background="Transparent"
                                                   VerticalAlignment="Center"
                                                   Text="{Binding TagClipCount, NotifyOnSourceUpdated=True, NotifyOnTargetUpdated=True}"
                                                   Foreground="{Binding TagCountTextColor}" />
                                    </local:MpClipBorder>
                                    <TextBlock x:Name="TagTextBlock"
                                               Margin="3,0,0,0"
                                               Background="Transparent"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               Visibility="{Binding TextBlockVisibility}"
                                               FontSize="{Binding TagFontSize}"
                                               Foreground="{Binding TagTextColor}"
                                               Text="{Binding TagName}">
                                    </TextBlock>
                                    <TextBox x:Name="TagTextBox"
                                             Margin="3,0,0,0"
                                             Background="White"
                                             Foreground="Black"
                                             IsReadOnly="False"
                                             HorizontalAlignment="Center"
                                             VerticalAlignment="Center"
                                             local:MpFocusExtension.IsFocused="{Binding IsTextBoxFocused}"
                                             Visibility="{Binding TextBoxVisibility}"
                                             FontSize="{Binding TagFontSize}"
                                             Text="{Binding TagName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <!-- Add Tag Button -->
                <Button Height="30"
                        Width="30" 
                        BorderThickness="0"
                        Background="Transparent"
                        Command="{Binding CreateTagCommand}">
                    <Image Stretch="Fill"
                           Source="/MpWpfApp;component/Resources/add4.png" />
                </Button>
            </StackPanel>
            <!-- Search Border -->
            <local:MpClipBorder Grid.Row="0"
                                Grid.Column="2"
                                Margin="0"
                                BorderBrush="{Binding SearchTextBoxBorderBrush}"
                                BorderThickness="5"
                                Focusable="False"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Stretch"
                                Background="White"
                                CornerRadius="15">
                <StackPanel Margin="5"
                            Orientation="Horizontal">
                    <Button Background="Transparent"
                            BorderThickness="0">
                        <Image Source="pack://application:,,,/Resources/search.png"
                               Stretch="Fill" />
                    </Button>
                    <!-- Search TextBox-->
                    <TextBox x:Name="SearchTextBox"
                             Text="{Binding SearchText,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,NotifyOnTargetUpdated=True,NotifyOnSourceUpdated=True}"
                             FontStyle="{Binding SearchTextBoxFontStyle}"
                             Foreground="{Binding SearchTextBoxTextBrush}"
                             local:MpFocusExtension.IsFocused="{Binding IsSearchTextBoxFocused, NotifyOnSourceUpdated=True,NotifyOnTargetUpdated=True,UpdateSourceTrigger=PropertyChanged}"
                             BorderThickness="0"
                             FontSize="18"
                             MinWidth="1000"
                             VerticalAlignment="Top"
                             HorizontalAlignment="Left"
                             VerticalContentAlignment="Top">
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="GotFocus">
                                <i:CallMethodAction TargetObject="{Binding}"
                                                    MethodName="SearchTextboxGotFocus" />
                            </i:EventTrigger>
                            <i:EventTrigger EventName="LostFocus">
                                <i:CallMethodAction TargetObject="{Binding}"
                                                    MethodName="SearchTextboxLostFocus" />
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </TextBox>
                </StackPanel>
            </local:MpClipBorder>
        </Grid>
        <!--3rd row row for tray -->
        <Grid Grid.Row="2"
              Grid.Column="0"              
              Focusable="True"
              Background="MediumPurple">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="{Binding AppStateButtonGridWidth}" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <!-- Side bar AppState Buttons -->
            <Grid Grid.Row="0"
                  Grid.Column="0"
                  Background="Orange">
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <ToggleButton Grid.Row="0"
                              Grid.Column="0"
                              Padding="0,5,0,5">
                    <TextBlock Text="Pause System"
                               HorizontalAlignment="Center"
                               TextWrapping="Wrap" />
                </ToggleButton>
                <ToggleButton Grid.Row="1"
                              Grid.Column="0"
                              IsChecked="{Binding IsAutoCopyMode}"
                              Padding="0,5,0,5">
                    <TextBlock Text="Auto Copy"
                               HorizontalAlignment="Center"
                               TextWrapping="Wrap" />
                </ToggleButton>
                <ToggleButton Grid.Row="2"
                              Grid.Column="0"
                              Padding="0,5,0,5">
                    <TextBlock Text="Auto Paste"
                               HorizontalAlignment="Center"
                               TextWrapping="Wrap" />
                </ToggleButton>
                <ToggleButton Grid.Row="3"
                              Grid.Column="0"
                              Padding="0,5,0,5"
                              IsChecked="{Binding IsInAppendMode}">
                    <TextBlock Text="Auto Append"
                               FontSize="11"
                               HorizontalAlignment="Center"
                               TextWrapping="Wrap" />
                </ToggleButton>
                <ToggleButton Grid.Row="4"
                              Grid.Column="0"
                              Padding="0,10,10,10">
                    <TextBlock Text="Smart Copy"
                               HorizontalAlignment="Center"
                               TextWrapping="Wrap" />
                </ToggleButton>
            </Grid>
            <!-- tile tray -->
            <ListBox x:Name="ClipTray"
                     Grid.Row="0"
                     Grid.Column="1"                     
                     dd:DragDrop.IsDragSource="True"
                     dd:DragDrop.IsDropTarget="False"
                     dd:DragDrop.DragHandler="{Binding}"
                     VerticalAlignment="Stretch"
                     Background="Transparent"
                     SelectionMode="Extended"
                     VirtualizingPanel.IsVirtualizing="True"
                     VirtualizingPanel.ScrollUnit="Pixel"
                     VirtualizingPanel.VirtualizationMode="Recycling"
                     ScrollViewer.CanContentScroll="False"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto"
                     ScrollViewer.VerticalScrollBarVisibility="Hidden"
                     Visibility="{Binding ClipListVisibility}"
                     ItemsSource="{Binding ClipTiles}">
                <i:Interaction.Triggers>
                    <!--<i:EventTrigger EventName="Drop">
                        <i:CallMethodAction TargetObject="{Binding}" 
                                                        MethodName="ClipTray_Drop"/>
                    </i:EventTrigger>-->
                    <i:EventTrigger EventName="PreviewMouseWheel">
                        <i:CallMethodAction MethodName="ScrollClipTray"
                                            TargetObject="{Binding}" />
                    </i:EventTrigger>
                    <i:EventTrigger EventName="SelectionChanged">
                        <i:CallMethodAction TargetObject="{Binding}"
                                            MethodName="ClipTray_SelectionChanged" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="{x:Type ListBoxItem}">
                        <Setter Property="IsSelected"
                                Value="{Binding IsSelected}" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                    <ContentPresenter />
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected"
                                                 Value="True">
                                            <Setter Property="BorderBrush"
                                                    Value="Transparent" />
                                            <Setter Property="Background"
                                                    Value="Transparent" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel IsItemsHost="True"                                                
                                                CanVerticallyScroll="False"                                                
                                                VerticalAlignment="Top"
                                                Orientation="Horizontal">
                        </VirtualizingStackPanel>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <!-- Clip Tile -->
                        <local:MpClipBorder BorderBrush="{Binding TileBorderBrush}"
                                            Focusable="True"                                            
                                            local:MpFocusExtension.IsFocused="{Binding IsFocused}"
                                            Margin="{Binding TileMargin}"
                                            Background="White"
                                            BorderThickness="{Binding TileBorderThickness}"
                                            Width="{Binding TileBorderSize}"
                                            Height="{Binding TileBorderSize}"
                                            CornerRadius="10"
                                            Visibility="{Binding TileVisibility, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged,NotifyOnSourceUpdated=True}">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Loaded">
                                    <i:CallMethodAction TargetObject="{Binding}"
                                                        MethodName="ClipTile_Loaded" />
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                            <local:MpClipBorder.ContextMenu>
                                <ContextMenu x:Name="ClipTile_ContextMenu">
                                    <MenuItem Header="Bring To Front"
                                              Command="{Binding MainWindowViewModel.BringSelectedClipTilesToFrontCommand}" />
                                    <MenuItem Header="Export To">
                                        <MenuItem Header="File(s)..."
                                                  Command="{Binding MainWindowViewModel.ExportSelectedClipTilesCommand}"
                                                  CommandParameter="{StaticResource False}"/>
                                        <MenuItem Header="CSV"
                                                  Command="{Binding MainWindowViewModel.ExportSelectedClipTilesCommand}"
                                                  CommandParameter="{StaticResource True}"/>
                                    </MenuItem>
                                    <MenuItem Header="Rename"
                                              Command="{Binding MainWindowViewModel.RenameClipCommand}" />
                                    <MenuItem Header="Delete"
                                              Command="{Binding MainWindowViewModel.DeleteSelectedClipsCommand}" />
                                    <MenuItem Header="Say"
                                              Command="{Binding SpeakClipCommand}" />
                                    <MenuItem Header="Merge"
                                              Command="{Binding MainWindowViewModel.MergeClipsCommand}"
                                              Visibility="{Binding MainWindowViewModel.MergeClipsCommandVisibility}" />
                                    <MenuItem Header="Search Web">
                                        <MenuItem Header="Google">
                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="PreviewMouseLeftButtonUp">
                                                    <i:CallMethodAction MethodName="ContextMenuMouseLeftButtonUpOnSearchGoogle"
                                                                        TargetObject="{Binding}" />
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>
                                        </MenuItem>
                                        <MenuItem Header="Bing">
                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="PreviewMouseLeftButtonUp">
                                                    <i:CallMethodAction MethodName="ContextMenuMouseLeftButtonUpOnSearchBing"
                                                                        TargetObject="{Binding}" />
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>
                                        </MenuItem>
                                        <MenuItem Header="DuckDuckGo">
                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="PreviewMouseLeftButtonUp">
                                                    <i:CallMethodAction MethodName="ContextMenuMouseLeftButtonUpOnSearchDuckDuckGo"
                                                                        TargetObject="{Binding}" />
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>
                                        </MenuItem>
                                        <MenuItem Header="Yandex">
                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="PreviewMouseLeftButtonUp">
                                                    <i:CallMethodAction MethodName="ContextMenuMouseLeftButtonUpOnSearchYandex"
                                                                        TargetObject="{Binding}" />
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>
                                        </MenuItem>
                                    </MenuItem>
                                    <MenuItem Header="Pin To"
                                              ItemsSource="{Binding TagMenuItems}">
                                        <MenuItem.ItemContainerStyle>
                                            <Style TargetType="{x:Type MenuItem}">
                                                <Setter Property="Header"
                                                        Value="{Binding Header}" />
                                                <Setter Property="Command"
                                                        Value="{Binding Command}" />
                                                <Setter Property="CommandParameter"
                                                        Value="{Binding TagViewModel}" />
                                                <Setter Property="IsCheckable"
                                                        Value="True" />
                                                <Setter Property="IsChecked"
                                                        Value="{Binding IsTagLinkedToClip}" />
                                            </Style>
                                        </MenuItem.ItemContainerStyle>
                                    </MenuItem>
                                </ContextMenu>
                            </local:MpClipBorder.ContextMenu>
                            <local:MpClipBorder.Effect>
                                <DropShadowEffect Color="Black"
                                                  BlurRadius="10" />
                            </local:MpClipBorder.Effect>
                            <Grid ClipToBounds="False">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="{Binding TileTitleHeight}" />
                                    <RowDefinition />
                                </Grid.RowDefinitions>
                                <!-- Tile Title StackPanel -->
                                <Canvas Grid.Row="0"
                                        Grid.Column="0"
                                        VerticalAlignment="Top"
                                        HorizontalAlignment="Left">
                                    <local:MpTintedImage x:Name="TitleSwirl0"
                                                         TintBrush="{Binding TitleColor,Converter={StaticResource SolidColorBrushToBrushConverter}}"
                                                         Canvas.Top="-1"
                                                         Canvas.Left="-1"
                                                         Stretch="Fill"
                                                         Width="{Binding TileSize}"
                                                         Height="{Binding TileTitleHeight}"
                                                         Source="/MpWpfApp;component/Resources/title_swirl0001.png" />
                                    <local:MpTintedImage x:Name="TitleSwirl1"
                                                         TintBrush="{Binding TitleColorLighter,Converter={StaticResource SolidColorBrushToBrushConverter}}"
                                                         Canvas.Top="-1"
                                                         Canvas.Left="-1"
                                                         Stretch="Fill"
                                                         Width="{Binding TileSize}"
                                                         Height="{Binding TileTitleHeight}"
                                                         Source="/MpWpfApp;component/Resources/title_swirl0002.png" />
                                    <local:MpTintedImage x:Name="TitleSwirl2"
                                                         TintBrush="{Binding TitleColorDarker, Converter={StaticResource SolidColorBrushToBrushConverter}}"
                                                         Canvas.Top="-1"
                                                         Canvas.Left="-1"
                                                         Stretch="Fill"
                                                         Width="{Binding TileSize}"
                                                         Height="{Binding TileTitleHeight}"
                                                         Source="/MpWpfApp;component/Resources/title_swirl0003.png" />
                                    <local:MpTintedImage x:Name="TitleSwirl3"
                                                         TintBrush="{Binding TitleColorAccent, Converter={StaticResource SolidColorBrushToBrushConverter}}"
                                                         Canvas.Top="-1"
                                                         Canvas.Left="-1"
                                                         Stretch="Fill"
                                                         Width="{Binding TileSize}"
                                                         Height="{Binding TileTitleHeight}"
                                                         Source="/MpWpfApp;component/Resources/title_swirl0004.png" />
                                    <TextBlock x:Name="ClipTileTitleTextBlock"
                                               Text="{Binding Title}"
                                               Canvas.Top="-2"
                                               Canvas.Left="5"
                                               FontSize="24"
                                               Foreground="White"
                                               Background="Transparent"
                                               Visibility="{Binding TileTitleTextBlockVisibility}">
                                        <TextBlock.Effect>
                                            <DropShadowEffect ShadowDepth="2"
                                                              Direction="330"
                                                              Color="Black"
                                                              Opacity="1.0"
                                                              BlurRadius="7" />
                                        </TextBlock.Effect>
                                    </TextBlock>
                                    <!-- Clip Title TextBox -->
                                    <TextBox x:Name="ClipTileTitleTextBox"
                                             Background="White"
                                             Foreground="Black"
                                             Padding="10,0,0,0"
                                             FontSize="24"
                                             Visibility="{Binding TileTitleTextBoxVisibility}"
                                             local:MpFocusExtension.IsFocused="{Binding IsTitleTextBoxFocused}"
                                             local:MpTextBoxBehavior.SelectAllTextOnFocus="True"
                                             Text="{Binding Title, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                    <!-- Clip Title Icon Image -->
                                    <Image x:Name="ClipTileAppIconImage"
                                           Source="{Binding Icon}"
                                           Stretch="Fill"
                                           Width="{Binding TileTitleIconSize}"
                                           Height="{Binding TileTitleIconSize}">
                                        <Image.Effect>
                                            <DropShadowEffect Color="DimGray" />
                                        </Image.Effect>
                                    </Image>
                                </Canvas>
                                <!-- RichText Content -->
                                <local:MpClipTileRichTextBox x:Name="ClipTileRichTextBox"
                                                             SearchText="{Binding MainWindowViewModel.SearchText}"
                                                             IsReadOnly="True"
                                                             Grid.Row="1"
                                                             Grid.Column="0"
                                                             Background="White"
                                                             VerticalAlignment="Top"
                                                             HorizontalAlignment="Left"
                                                             MinWidth="{Binding TileBorderSize}"
                                                             MinHeight="{Binding TileContentHeight}"
                                                             ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                                                             ScrollViewer.VerticalScrollBarVisibility="Hidden"
                                                             ScrollViewer.IsDeferredScrollingEnabled="True"
                                                             IsDocumentEnabled="True"
                                                             Focusable="False"
                                                             Cursor="Arrow"
                                                             BorderThickness="0"
                                                             Padding="10" />
                                <!-- Image Content-->
                                <Image Grid.Row="1"
                                       Grid.Column="0"
                                       x:Name="ClipTileImage"
                                       Focusable="False"
                                       Width="{Binding TileBorderSize}"
                                       Height="{Binding TileContentHeight}"
                                       Stretch="Fill" />
                                <!--File List Content -->
                                <ListBox Grid.Row="1"
                                         Grid.Column="0"
                                         BorderThickness="0"
                                         x:Name="ClipTileFileListBox"
                                         Width="{Binding TileBorderSize}"
                                         Height="{Binding TileContentHeight}"
                                         ItemsSource="{Binding FileListItems}">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <Border Height="22"
                                                    BorderThickness="0">
                                                <StackPanel VerticalAlignment="Center"
                                                            HorizontalAlignment="Left"
                                                            Orientation="Horizontal">
                                                    <Image Source="{Binding Icon}"
                                                           Height="22"
                                                           Stretch="Fill">
                                                    </Image>
                                                    <TextBlock Text="{Binding ItemPath}"
                                                               Margin="0,0,0,0"
                                                               VerticalAlignment="Center"
                                                               FontSize="16"
                                                               Foreground="Black"
                                                               Background="Transparent">
                                                    </TextBlock>
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </Grid>
                        </local:MpClipBorder>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <TextBlock x:Name="EmptyListTextBlock"
                       Grid.Row="0"
                       Grid.Column="1"
                       Text="This list has no items"
                       Focusable="True"
                       Visibility="{Binding EmptyListMessageVisibility}"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Foreground="LightGray"
                       Margin="545,91,683,132"
                       FontStyle="Oblique"
                       FontSize="34" />
        </Grid>
    </Grid>
</Window>
