<Window x:Class="MpWpfApp.MpTemplateTokenPasteModalWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MpWpfApp"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}"
        Height="300"
        Width="450"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:CallMethodAction MethodName="TemplateTokenModalWindow_Loaded"
                                TargetObject="{Binding}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.5*"/>
            <RowDefinition Height="0.7*"/>
            <RowDefinition Height="*" />
            <RowDefinition Height="0.7*"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0"
               FontSize="20"
               FontWeight="Bold"
               HorizontalAlignment="Stretch"
               HorizontalContentAlignment="Center"
               VerticalAlignment="Center"
               Content="Define Template" />
        <Grid Grid.Row="1" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.2*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="0.2*" />
            </Grid.ColumnDefinitions>
            <Button x:Name="PreviousTokenButton"
                    Margin="10"
                    Grid.Column="0"
                    Command="{Binding PreviousTokenCommand}">
                <Image Source="/Resources/Images/back_black.png"
                       Stretch="Fill"
                       SnapsToDevicePixels="True" />
            </Button>
            <ComboBox x:Name="TemplateSelectorComboBox"
                      Grid.Column="1"
                      Margin="10"
                      FontSize="16"
                      HorizontalContentAlignment="Left"
                      SelectedIndex="{Binding SelectedTokenIdx}"
                      ItemsSource="{Binding TemplateTokenList}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding}"
                                       FontSize="16"
                                       Height="20" />
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <Button x:Name="NextTokenButton"
                    Margin="10"
                    Grid.Column="2"
                    Command="{Binding NextTokenCommand}">
                <Image Source="/Resources/Images/forward_black.png"
                       Stretch="Fill"
                       SnapsToDevicePixels="True" />
            </Button>
        </Grid>
        <TextBox x:Name="TemplateNameEditorTextBox"
                 Grid.Row="2"
                 Margin="25"
                 Foreground="Black"
                 VerticalAlignment="Stretch"
                 HorizontalAlignment="Center"
                 HorizontalContentAlignment="Center"
                 VerticalContentAlignment="Center"
                 BorderBrush="Black"
                 BorderThickness="1"
                 Text="{Binding SelectedTokenText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, NotifyOnTargetUpdated=True, NotifyOnSourceUpdated=True}"
                 FontSize="18"
                 MinWidth="275"
                 IsReadOnly="False"/>
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Margin="10,10,10,10"
                    Grid.Column="0"
                    FontSize="18"
                    Content="Cancel"
                    IsCancel="True"
                    HorizontalAlignment="Stretch"
                    Command="{Binding CancelCommand}" />
            <Button Margin="10,10,10,10.5"
                    Grid.Column="3"
                    FontSize="18"
                    Content="Ok"
                    HorizontalAlignment="Stretch"
                    Command="{Binding OkCommand}"
                    IsDefault="True" />
        </Grid>
    </Grid>
</Window>
