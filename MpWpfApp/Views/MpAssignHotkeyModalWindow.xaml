<Window x:Class="MpWpfApp.MpAssignHotkeyModalWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MpWpfApp"
        mc:Ignorable="d"
        Title="Assign Hotkey"
        Height="250"
        Width="250"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <local:MpAssignHotkeyModalWindowViewModel />
    </Window.DataContext>
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:CallMethodAction MethodName="AssignHotkeyModalWindow_Loaded"
                                TargetObject="{Binding}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Label Grid.Row="0"
               FontSize="16"
               HorizontalAlignment="Center"
               HorizontalContentAlignment="Center"
               Content="Press Keys for" />
        <Label Grid.Row="1"
               FontSize="16"
               HorizontalAlignment="Center"
               HorizontalContentAlignment="Center"
               Content="{Binding CommandTypeName}" />
        <TextBlock Grid.Row="2"
               FontSize="14"
               FontStyle="Oblique"
               HorizontalAlignment="Center"
               Text="{Binding KeysString}" />
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Button Margin="10,15,10,15" Grid.Column="0" Content="Cancel" HorizontalAlignment="Stretch" Command="{Binding CancelCommand}"/>
            <Button Margin="10,15,10,15" Grid.Column="1" Content="Reset" HorizontalAlignment="Stretch" Command="{Binding ResetCommand}"/>
            <Button Margin="10,15,10,15" Grid.Column="2" Content="Ok" HorizontalAlignment="Stretch" Command="{Binding OkCommand}"/>
        </Grid>
    </Grid>
</Window>
