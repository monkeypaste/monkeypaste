<local:MpWindow x:Class="MpWpfApp.MpSettingsWindow"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
                xmlns:ii="http://schemas.microsoft.com/expression/2010/interactivity"
                xmlns:MVVVMLightCommand="clr-namespace:GalaSoft.MvvmLight.Command;assembly=GalaSoft.MvvmLight.Platform"
                xmlns:s="clr-namespace:System;assembly=mscorlib"
                xmlns:tb="http://www.hardcodet.net/taskbar"
                xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
                xmlns:dd="clr-namespace:GongSolutions.Wpf.DragDrop;assembly=GongSolutions.Wpf.DragDrop"
                xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                xmlns:local="clr-namespace:MpWpfApp"
                x:TypeArguments="local:MpSettingsWindowViewModel"
                mc:Ignorable="d"
                Title="Settings"
                Height="650"
                Width="950"
                SizeToContent="Manual"
                ShowInTaskbar="False"
                Topmost="True"
                WindowStartupLocation="CenterScreen"
                d:DataContext="{d:DesignInstance Type=local:MpSettingsWindowViewModel,
                                                 IsDesignTimeCreatable=False}">
    <Window.DataContext>
        <local:MpSettingsWindowViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.25*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="0.15*" />
        </Grid.RowDefinitions>
        <!--  Header Panel  -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <!--  Account Tab Button  -->
            <ToggleButton Grid.Column="0"
                          IsChecked="{Binding SettingsPanel1Visibility,
                                              Converter={StaticResource VisibilityToBoolConverter}}"
                          Command="{Binding ClickSettingsPanelCommand}"
                          CommandParameter="{StaticResource SettingsTabAccount}">
                <StackPanel Orientation="Vertical">
                    <Image Stretch="Fill"
                           Height="50"
                           Width="50"
                           Margin="0,10,0,0"
                           Source="/MpWpfApp;component/Resources/Images/user.png" />
                    <Label HorizontalAlignment="Center"
                           FontSize="16"
                           Content="Account" />
                </StackPanel>
            </ToggleButton>
            <!--  Prefs Tab Button  -->
            <ToggleButton Grid.Column="1"
                          IsChecked="{Binding SettingsPanel2Visibility,
                                              Converter={StaticResource VisibilityToBoolConverter}}"
                          Command="{Binding ClickSettingsPanelCommand}"
                          CommandParameter="{StaticResource SettingsTabPreferences}">
                <StackPanel Orientation="Vertical">
                    <Image Stretch="Fill"
                           Height="50"
                           Width="50"
                           Margin="0,10,0,0"
                           Source="/MpWpfApp;component/Resources/Images/sliders.png" />
                    <Label HorizontalAlignment="Center"
                           FontSize="16"
                           Content="Preferences" />
                </StackPanel>
            </ToggleButton>
            <!--  Security Tab Button  -->
            <ToggleButton Grid.Column="2"
                          IsChecked="{Binding SettingsPanel3Visibility,
                                              Converter={StaticResource VisibilityToBoolConverter}}"
                          Command="{Binding ClickSettingsPanelCommand}"
                          CommandParameter="{StaticResource SettingsTabSecurity}">
                <StackPanel Orientation="Vertical">
                    <Image Stretch="Fill"
                           Height="50"
                           Width="50"
                           Margin="0,10,0,0"
                           Source="/MpWpfApp;component/Resources/Images/shield.png" />
                    <Label HorizontalAlignment="Center"
                           FontSize="16"
                           Content="Security" />
                </StackPanel>
            </ToggleButton>
            <!--  Shortcuts tab button  -->
            <ToggleButton Grid.Column="3"
                          IsChecked="{Binding SettingsPanel4Visibility,
                                              Converter={StaticResource VisibilityToBoolConverter}}"
                          Command="{Binding ClickSettingsPanelCommand}"
                          CommandParameter="{StaticResource SettingsTabShortcuts}">
                <StackPanel Orientation="Vertical">
                    <Image Stretch="Fill"
                           Height="50"
                           Width="50"
                           Margin="0,10,0,0"
                           Source="/MpWpfApp;component/Resources/Images/joystick.png" />
                    <Label HorizontalAlignment="Center"
                           FontSize="16"
                           Content="Shortcuts" />
                </StackPanel>
            </ToggleButton>
            <!--  Help tab Button  -->
            <ToggleButton Grid.Column="4"
                          IsChecked="{Binding SettingsPanel5Visibility,
                                              Converter={StaticResource VisibilityToBoolConverter}}"
                          Command="{Binding ClickSettingsPanelCommand}"
                          CommandParameter="{StaticResource SettingsTabHelp}">
                <StackPanel Orientation="Vertical">
                    <Image Stretch="Fill"
                           Height="50"
                           Width="50"
                           Margin="0,10,0,0"
                           Source="/MpWpfApp;component/Resources/Images/info.png" />
                    <Label HorizontalAlignment="Center"
                           FontSize="16"
                           Content="Help" />
                </StackPanel>
            </ToggleButton>
        </Grid>
        <!--  Account Panel  -->
        <StackPanel Grid.Row="1"
                    Visibility="{Binding SettingsPanel1Visibility}">
            <local:MpAccountTabPanel DataContext="{Binding}" />
        </StackPanel>
        <!--  Preferences  -->
        <StackPanel Grid.Row="1"
                    Visibility="{Binding SettingsPanel2Visibility}">
            <local:MpPreferencesTabPanel DataContext="{Binding PreferencesViewModel}" />
        </StackPanel>
        <!--  Security  -->
        <StackPanel Grid.Row="1"
                    Visibility="{Binding SettingsPanel3Visibility}">
            <local:MpSecurityTabPanel DataContext="{Binding}" />
        </StackPanel>
        <!--  Shortcuts  -->
        <local:MpShortcutsTabPanel Grid.Row="1"
                                   Visibility="{Binding SettingsPanel4Visibility}"
                                   DataContext="{Binding}" />
        <!--  Help  -->
        <StackPanel Grid.Row="1"
                    Visibility="{Binding SettingsPanel5Visibility}">
            <local:MpHelpTabPanel DataContext="{Binding}" />
        </StackPanel>
        <!--  Footer  -->
        <DockPanel Grid.Row="2"
                   HorizontalAlignment="Stretch">
            <Button x:Name="ResetButton"
                    DockPanel.Dock="Left"
                    Width="100"
                    Margin="10,20,10,10"
                    Click="ResetButton_Click"
                    Content="Reset All" />
            <StackPanel DockPanel.Dock="Right"
                        HorizontalAlignment="Right"
                        Orientation="Horizontal">
                <Button x:Name="SaveButton"
                        Width="100"
                        Margin="10,20,10,10"
                        IsDefault="True"
                        Click="SaveButton_Click"
                        Content="Save" />
                <Button x:Name="CancelButton"
                        Width="100"
                        Margin="10,20,10,10"
                        Click="CancelButton_Click"
                        Content="Cancel" />
            </StackPanel>
        </DockPanel>
    </Grid>
</local:MpWindow>
