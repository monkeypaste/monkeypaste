<UserControl x:Class="MpWpfApp.MpShortcutsTabPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MpWpfApp"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.1*" />
            <RowDefinition Height="0.1*" />
            <RowDefinition Height="0.3*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0"
                    Visibility="Collapsed"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center">
            <Image Width="30"
                   Source="/MpWpfApp;component/Resources/Images/info.png" />
            <Label HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Content="Sequence changes will take affect upon application restart" />
        </StackPanel>
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal">
            <Label Content="Show Window With Mouse" />
            <ComboBox SelectedIndex="{local:MpSettingBinding DoShowMainWIndowWithMouseEdgeIndex}">
                <ComboBoxItem Content="Left" />
                <ComboBoxItem Content="Top" />
                <ComboBoxItem Content="Right" />
                <ComboBoxItem Content="Bottom" />
                <ComboBoxItem Content="None" />
            </ComboBox>
        </StackPanel>
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal">
            <Label Content="Only with Scrollwheel" />
            <CheckBox IsChecked="{local:MpSettingBinding Path=DoShowMainWindowWithMouseEdgeAndScrollDelta}" />
        </StackPanel>
        <local:MpShortcutCollectionDataGridView Grid.Row="3"
                                                DataContext="{StaticResource ShortcutCollectionViewModel}" />
    </Grid>
</UserControl>
