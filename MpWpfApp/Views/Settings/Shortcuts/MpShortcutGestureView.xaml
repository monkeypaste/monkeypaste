<local:MpUserControl x:Class="MpWpfApp.MpShortcutGestureView"
                     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                     xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                     xmlns:local="clr-namespace:MpWpfApp"
                     x:TypeArguments="local:MpIShortcutCommand"
                     mc:Ignorable="d"
                     d:DesignHeight="50"
                     d:DesignWidth="800"
                     d:DataContext="{d:DesignInstance Type=local:MpIShortcutCommand,
                                                      IsDesignTimeCreatable=False}">
    <Grid MouseEnter="Grid_MouseEnter"
          MouseLeave="Grid_MouseLeave">
        <ListBox x:Name="ShortcutListBox"
                 DataContext="{Binding ShortcutViewModel}"
                 IsEnabled="False"
                 BorderThickness="0"
                 HorizontalAlignment="Center"
                 ScrollViewer.VerticalScrollBarVisibility="Hidden"
                 ScrollViewer.HorizontalScrollBarVisibility="Auto"
                 Visibility="{Binding IsEmpty,
                                      Converter={StaticResource BoolToVisibilityFlipConverter}}"
                 ItemsSource="{Binding KeyItems}">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel Orientation="Horizontal"
                                            IsItemsHost="True"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Center" />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemContainerStyle>
                <Style TargetType="{x:Type ListBoxItem}">
                    <Setter Property="FocusVisualStyle"
                                        Value="{x:Null}" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                <ContentPresenter />
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected"
                                                         Value="True">
                                        <Setter Property="BorderBrush"
                                                            Value="Transparent" />
                                        <Setter Property="Background"
                                                            Value="Transparent" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Black"
                            BorderThickness="0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="auto" />
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0"
                                    HorizontalAlignment="Left"
                                    Background="LightGray"
                                    BorderBrush="DarkGray"
                                    Margin="3"
                                    BorderThickness="2"
                                    CornerRadius="5">
                                <TextBlock HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           Padding="3,0"
                                           FontSize="10"
                                           Foreground="Black"
                                           Text="{Binding KeyStr}"
                                           FontWeight="Bold" />
                            </Border>
                            <Label x:Name="ComboLabel"
                                   Visibility="{Binding PlusVisibility}"
                                   Content="+"
                                   Grid.Column="1"
                                   FontSize="14"
                                   FontWeight="Bold"
                                   HorizontalAlignment="Left"
                                   VerticalAlignment="Center" />
                            <!--<Label x:Name="SequenceLabel"
                                       Visibility="{Binding CommaVisibility}"
                                       Content=","
                                       Grid.Column="1"
                                       FontSize="20"
                                       FontWeight="Bold"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Bottom" />-->
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.GroupStyle>
                <GroupStyle>
                    <GroupStyle.Panel>
                        <ItemsPanelTemplate>
                            <VirtualizingStackPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </GroupStyle.Panel>
                    <GroupStyle.ContainerStyle>
                        <Style TargetType="{x:Type GroupItem}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate>
                                        <Border CornerRadius="5"
                                                Margin="5,0,5,0"
                                                Padding="3"
                                                BorderBrush="LimeGreen"
                                                BorderThickness="1"
                                                Background="Aquamarine">
                                            <ItemsPresenter />
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </GroupStyle.ContainerStyle>
                </GroupStyle>
            </ListBox.GroupStyle>
        </ListBox>
        <Button x:Name="AssignButton"
                Background="Transparent"
                Command="{Binding AssignCommand}">
            <Border CornerRadius="10"
                    DataContext="{Binding ShortcutViewModel}"
                    BorderThickness="1"
                    BorderBrush="DimGray"
                    Background="LightGray">
                <StackPanel Orientation="Horizontal"
                            Visibility="{Binding IsEmpty,
                                                 Converter={StaticResource BoolToVisibilityConverter}}">
                    <Ellipse x:Name="RecordEllipse"
                             Width="15"
                             Height="15"
                             Fill="Red"
                             Margin="5,3"
                             HorizontalAlignment="Center"
                             VerticalAlignment="Center" />
                    <TextBlock x:Name="EmptyGestureTextBlock"
                               Margin="0,0,10,0"
                               VerticalAlignment="Center"
                               Text="Click To Set"
                               FontSize="12"
                               FontStyle="Italic"
                               Foreground="DimGray"
                               Background="Transparent" />
                </StackPanel>
            </Border>
        </Button>

    </Grid>
</local:MpUserControl>
