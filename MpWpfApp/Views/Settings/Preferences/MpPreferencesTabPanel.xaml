<UserControl x:Class="MpWpfApp.MpPreferencesTabPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MpWpfApp"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <StackPanel Margin="10"
                Orientation="Vertical"
                Grid.Column="0">
        <StackPanel Orientation="Horizontal">
            <Label Content="Monitor" />
            <ComboBox SelectedIndex="{local:MpSettingBinding MainWindowMonitorIdx}">
                <ComboBoxItem Content="Primary" />
                <ComboBoxItem Content="Focused" />
                <ComboBoxItem Content="Mouse" />
                <ComboBoxItem Content="Other..." />
            </ComboBox>
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <Label Content="Sound Notifications" />
            <ComboBox DataContext="{Binding SoundPlayerGroupCollectionViewModel}"
                      ItemsSource="{Binding SoundGroupNames}"
                      SelectedIndex="{Binding SelectedSoundGroupNameIdx}" />
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <Label Content="Load On Login" />
            <CheckBox IsChecked="{Binding IsLoadOnLoginChecked,
                                          Mode=TwoWay,
                                          UpdateSourceTrigger=PropertyChanged,
                                          NotifyOnTargetUpdated=True,
                                          NotifyOnSourceUpdated=True}" />
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <Label Content="Spell Check" />
            <CheckBox IsChecked="{Binding UseSpellCheck,
                                          Mode=TwoWay,
                                          UpdateSourceTrigger=PropertyChanged,
                                          NotifyOnTargetUpdated=True,
                                          NotifyOnSourceUpdated=True}" />
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <Label Content="Ignore Empty or whitespace clipboard items" />
            <CheckBox IsChecked="{Binding IgnoreWhiteSpaceCopyItems,
                                          Mode=TwoWay,
                                          UpdateSourceTrigger=PropertyChanged,
                                          NotifyOnTargetUpdated=True,
                                          NotifyOnSourceUpdated=True}" />
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <Label Content="Reset Clipboard After Paste" />
            <CheckBox IsChecked="{Binding ResetClipboard,
                                          Mode=TwoWay,
                                          UpdateSourceTrigger=PropertyChanged,
                                          NotifyOnTargetUpdated=True,
                                          NotifyOnSourceUpdated=True}" />
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <Label Content="Show Item Preview" />
            <CheckBox IsChecked="{Binding ShowItemPreview,
                                          Mode=TwoWay,
                                          UpdateSourceTrigger=PropertyChanged,
                                          NotifyOnTargetUpdated=True,
                                          NotifyOnSourceUpdated=True}" />
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <Label Content="Language" />
            <ComboBox ItemsSource="{Binding Languages}"
                      SelectedItem="{Binding SelectedLanguage,
                                             Mode=TwoWay,
                                             UpdateSourceTrigger=PropertyChanged,
                                             NotifyOnTargetUpdated=True,
                                             NotifyOnSourceUpdated=True}" />
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <Label Content="Max Rtf Char Count" />
            <TextBox Text="{Binding MaxRtfCharCount,
                                    Mode=TwoWay,
                                    UpdateSourceTrigger=PropertyChanged,
                                    NotifyOnTargetUpdated=True,
                                    NotifyOnSourceUpdated=True}" />
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <Label Content="Ignore Duplicates" />
            <CheckBox IsChecked="{Binding IgnoreNewDuplicates,
                                          Mode=TwoWay,
                                          UpdateSourceTrigger=PropertyChanged,
                                          NotifyOnTargetUpdated=True,
                                          NotifyOnSourceUpdated=True}" />
        </StackPanel>
        <StackPanel Grid.Column="0"
                    Orientation="Horizontal">
            <Label Content="Speaking Voice" />
            <ComboBox ItemsSource="{Binding VoiceNames}"
                      SelectedItem="{Binding SelectedVoiceName,
                                             Mode=TwoWay,
                                             UpdateSourceTrigger=PropertyChanged,
                                             NotifyOnTargetUpdated=True,
                                             NotifyOnSourceUpdated=True}" />
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <Label Content="Show Append Buffer" />
            <CheckBox IsChecked="{local:MpSettingBinding Path=NotificationShowAppendBufferToast,
                                                         Mode=TwoWay,
                                                         UpdateSourceTrigger=PropertyChanged,
                                                         NotifyOnTargetUpdated=True,
                                                         NotifyOnSourceUpdated=True}" />
        </StackPanel>

        <local:MpPasteToAppPathDataGridView DataContext="{Binding PasteToAppPathViewModelCollection}" />

    </StackPanel>
</UserControl>
