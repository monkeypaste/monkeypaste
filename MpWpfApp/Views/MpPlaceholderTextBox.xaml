<UserControl x:Class="MpWpfApp.MpPlaceholderTextBox"
             x:Name="PlaceholderTextBoxUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MpWpfApp"
             mc:Ignorable="d" 
             d:DesignHeight="25" 
             d:DesignWidth="125">
    <UserControl.DataContext>
        <local:MpPlaceholderTextBoxViewModel />
    </UserControl.DataContext>
    <local:MpClipBorder x:Name="PlaceholderTextBoxBorder"
                        CornerRadius="{Binding CornerRadius}"
                        BorderBrush="{Binding TextBoxBorderBrush}"
                        Background="White"
                        BorderThickness="3">
        <i:Interaction.Triggers>
            <i:EventTrigger EventName="Loaded">
                <i:CallMethodAction MethodName="PlaceholderTextBoxBorder_Loaded"
                                    TargetObject="{Binding}" />
            </i:EventTrigger>
        </i:Interaction.Triggers>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="0.1*" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="PlaceholderTextBox"
                     Grid.Column="0"
                     FontSize="{Binding FontSize}"
                     Background="Transparent"
                     Foreground="{Binding TextBoxTextBrush}"
                     FontStyle="{Binding TextBoxFontStyle}"
                     BorderThickness="0"
                     VerticalContentAlignment="Center"
                     Padding="5,0,0,0"
                     local:MpFocusExtension.IsFocused="{Binding IsTextBoxFocused, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, NotifyOnTargetUpdated=True, NotifyOnSourceUpdated=True}"
                     Text="{Binding Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, NotifyOnTargetUpdated=True, NotifyOnSourceUpdated=True}">
            </TextBox>
            <Button x:Name="ClearTextBoxButton"
                    Grid.Column="1"
                    Width="15"
                    Height="15"
                    Visibility="{Binding ClearTextButtonVisibility}"
                    Command="{Binding ClearTextCommand}"
                    Template="{DynamicResource BackgroundButton}">
                <Button.Background>
                    <ImageBrush ImageSource="/MpWpfApp;component/Resources/Images/close2.png" />
                </Button.Background>
                <Button.Resources>
                    <ControlTemplate x:Key="BackgroundButton"
                                     TargetType="Button">
                        <Border x:Name="border" 
                                BorderThickness="0"
                                Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver"
                                     Value="True">
                                <Setter Property="Background"
                                        TargetName="border">
                                    <Setter.Value>
                                        <ImageBrush ImageSource="/MpWpfApp;component/Resources/Images/close2.png"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Resources>
            </Button>
        </Grid>
    </local:MpClipBorder>
</UserControl>
