<local:MpAvUserControl xmlns="https://github.com/avaloniaui"
                       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                       xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                       xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                       xmlns:local="using:MonkeyPaste.Avalonia"
                       xmlns:pcl="using:MonkeyPaste"
                       x:TypeArguments="local:MpAvClipTrayViewModel"
                       x:DataType="local:MpAvClipTrayViewModel"
                       x:CompileBindings="True"
                       DataContext="{x:Static local:MpAvClipTrayViewModel.Instance}"
                       mc:Ignorable="d"
                       d:DesignWidth="800"
                       d:DesignHeight="450"
                       x:Class="MonkeyPaste.Avalonia.MpAvSidebarButtonGroupView">
    <Grid x:Name="SidebarButtonGroupContainerGrid"
          Classes.horizontal="{Binding !IsVerticalOrientation, Source={x:Static local:MpAvMainWindowViewModel.Instance}}"
          IsTabStop="True"
          TabIndex="0"
          Focusable="True"
          HorizontalAlignment="Stretch"
          VerticalAlignment="Stretch"
          RowDefinitions="*,*,*,*,*">
        <ToggleButton x:Name="TagTreeToggleButton"
                      Grid.Row="0"
                      IsChecked="{Binding IsSelected, Source={x:Static local:MpAvTagTrayViewModel.Instance}}">
            <ToolTip.Tip>
                <local:MpAvToolTipView ToolTipText="Collection Tree" />
            </ToolTip.Tip>
            <PathIcon Data="{StaticResource BooksSvg}" />
        </ToggleButton>
        <ToggleButton x:Name="ClipboardHandlerToggleButton"
                      Grid.Row="1"
                      IsChecked="{Binding IsSelected, Source={x:Static local:MpAvClipboardHandlerCollectionViewModel.Instance}}">
            <ToolTip.Tip>
                <local:MpAvToolTipView ToolTipText="Clipboard Handlers" />
            </ToolTip.Tip>
            <PathIcon Data="{StaticResource ClipboardSvg}" />
        </ToggleButton>
        <ToggleButton x:Name="AnalyzerToggleButton"
                      Grid.Row="2"
                      IsChecked="{Binding IsSelected, Source={x:Static local:MpAvAnalyticItemCollectionViewModel.Instance}}">
            <ToolTip.Tip>
                <local:MpAvToolTipView ToolTipText="Analyze" />
            </ToolTip.Tip>
            <PathIcon Data="{StaticResource BrainOutlineSvg}" />
        </ToggleButton>
        <ToggleButton x:Name="ActionsToggleButton"
                      Grid.Row="3"
                      IsChecked="{Binding IsSelected, Source={x:Static local:MpAvTriggerCollectionViewModel.Instance}}">
            <ToolTip.Tip>
                <local:MpAvToolTipView ToolTipText="Actions" />
            </ToolTip.Tip>
            <PathIcon Data="{StaticResource BoltSvg}" />
        </ToggleButton>
        <ToggleButton x:Name="AppModeToggleButton"
                      Grid.Row="4"
                      IsEnabled="{Binding !IsAppPaused}">
            <ToolTip.Tip>
                <local:MpAvToolTipView ToolTipText="Clipboard Modes" />
            </ToolTip.Tip>
            <!--<ToggleButton.Resources>
                <StackPanel x:Key="AppModeToggleButtonStackPanel"
                            Orientation="Horizontal">
                    <ToggleButton x:Name="AppendModeToggleButton"
                                  Loaded="AppendModeToggleButton_Loaded"
                                  Grid.Row="4"
                                  Padding="10"

                                  VerticalAlignment="Bottom"
                                  HorizontalAlignment="Center"
                                  Template="{StaticResource AppModeToggleButtonControlTemplate_NOCHECKED_STYLE}"
                                  DataContext="{x:Static local:MpClipTrayViewModel.Instance}"
                                  ToolTip="Append to Clipboard"
                                  IsEnabled="{Binding IsAppPaused,
                                                      Converter={StaticResource BoolFlipConverter}}"
                                  PreviewMouseLeftButtonDown="ToggleButton_PreviewMouseLeftButtonDown">
                        <Grid>
                            <Popup IsOpen="False"
                                   AllowsTransparency="True">
                                <StackPanel Orientation="Vertical"
                                            Background="Transparent">
                                    <ToggleButton x:Name="AppendToggleButton"
                                                  Loaded="AppendToggleButton_Loaded"
                                                  PreviewMouseLeftButtonDown="ToggleButton_PreviewMouseLeftButtonDown"
                                                  DataContext="{x:Static local:MpClipTrayViewModel.Instance}"
                                                  Width="{Binding AppStateButtonPanelWidth,
                                                                  Source={x:Static local:MpMeasurements.Instance}}"
                                                  Height="{Binding AppStateButtonPanelWidth,
                                                                   Source={x:Static local:MpMeasurements.Instance}}"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Center"
                                                  ToolTip="{Binding .,
                                                                    Converter={StaticResource ShortcutTypeToStringConverter},
                                                                    ConverterParameter={x:Static pcl:MpShortcutType.ToggleAppendMode}}"
                                                  Padding="10">
                                        <Image Source="{Binding Source={StaticResource AppendRunImage},
                                                                Converter={StaticResource StringResourceConverter}}"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center" />
                                    </ToggleButton>
                                    <ToggleButton x:Name="AppendLineToggleButton"
                                                  Loaded="AppendLineToggleButton_Loaded"
                                                  PreviewMouseLeftButtonDown="ToggleButton_PreviewMouseLeftButtonDown"
                                                  DataContext="{x:Static local:MpClipTrayViewModel.Instance}"
                                                  Width="{Binding AppStateButtonPanelWidth,
                                                                  Source={x:Static local:MpMeasurements.Instance}}"
                                                  Height="{Binding AppStateButtonPanelWidth,
                                                                   Source={x:Static local:MpMeasurements.Instance}}"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Center"
                                                  Padding="10">
            -->
            <!--<ToggleButton.ToolTip>
                                            <TextBlock>
                                                <Run Text="Append Line Cut/Copy " />
                                                <Run Text="{Binding .,
                                                                    Converter={StaticResource ShortcutTypeToStringConverter},
                                                                    ConverterParameter={x:Static pcl:MpShortcutType.ToggleAppendLineMode}}" />
                                            </TextBlock>
                                        </ToggleButton.ToolTip>-->
            <!--
                                        <Image Source="{Binding Source={StaticResource AppendLineImage},
                                                                Converter={StaticResource StringResourceConverter}}"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center" />
                                    </ToggleButton>
                                </StackPanel>
                            </Popup>
                            <Image Source="{Binding Source={StaticResource AppendImage},
                                                    Converter={StaticResource StringResourceConverter}}"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center" />
                        </Grid>
                    </ToggleButton>
                    <ToggleButton x:Name="MouseModeToggleButton"
                                  Loaded="MouseModeToggleButton_Loaded"
                                  Grid.Row="3"
                                  ToolTip="Mouse Modes"
                                  VerticalAlignment="Center"
                                  HorizontalAlignment="Left"
                                  Template="{StaticResource AppModeToggleButtonControlTemplate_NOCHECKED_STYLE}"
                                  IsEnabled="{Binding IsAppPaused,
                                                      Converter={StaticResource BoolFlipConverter}}"
                                  PreviewMouseLeftButtonDown="ToggleButton_PreviewMouseLeftButtonDown"
                                  Padding="10">
                        <Grid>
                            <Popup IsOpen="False"
                                   AllowsTransparency="True">
                                <StackPanel Orientation="Vertical"
                                            Background="Transparent">
                                    <ToggleButton x:Name="AutoCopyToggleButton"
                                                  Loaded="AutoCopyToggleButton_Loaded"
                                                  Width="{Binding AppStateButtonPanelWidth,
                                                                  Source={x:Static local:MpMeasurements.Instance}}"
                                                  Height="{Binding AppStateButtonPanelWidth,
                                                                   Source={x:Static local:MpMeasurements.Instance}}"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Center"
                                                  IsEnabled="{Binding IsAppPaused,
                                                                      Converter={StaticResource BoolFlipConverter}}"
                                                  PreviewMouseLeftButtonDown="ToggleButton_PreviewMouseLeftButtonDown"
                                                  Padding="10">
                                        <ToggleButton.ToolTip>
                                            <TextBlock>
                                                <Run Text="Auto-Copy Selection " />

                                                <Run Text="{Binding .,
                                                                    Converter={StaticResource ShortcutTypeToStringConverter},
                                                                    ConverterParameter={x:Static pcl:MpShortcutType.ToggleAutoCopyMode}}" />
                                            </TextBlock>
                                        </ToggleButton.ToolTip>
                                        <Image Source="{Binding Source={StaticResource LeftClickImage},
                                                                Converter={StaticResource StringResourceConverter}}"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center" />
                                    </ToggleButton>
                                    <ToggleButton x:Name="RightClickPasteToggleButton"
                                                  Loaded="RightClickPasteToggleButton_Loaded"
                                                  Width="{Binding AppStateButtonPanelWidth,
                                                                  Source={x:Static local:MpMeasurements.Instance}}"
                                                  Height="{Binding AppStateButtonPanelWidth,
                                                                   Source={x:Static local:MpMeasurements.Instance}}"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Center"
                                                  IsEnabled="{Binding IsAppPaused,
                                                                      Converter={StaticResource BoolFlipConverter}}"
                                                  PreviewMouseLeftButtonDown="ToggleButton_PreviewMouseLeftButtonDown"
                                                  Padding="10">
                                        <ToggleButton.ToolTip>
                                            <TextBlock>
                                                <Run Text="Right-Click Paste " />
                                                <Run Text="{Binding .,
                                                                    Converter={StaticResource ShortcutTypeToStringConverter},
                                                                    ConverterParameter={x:Static pcl:MpShortcutType.ToggleRightClickPasteMode}}" />
                                            </TextBlock>
                                        </ToggleButton.ToolTip>
                                        <Image Source="{Binding Source={StaticResource RightClickImage},
                                                                Converter={StaticResource StringResourceConverter}}"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center" />
                                    </ToggleButton>
                                </StackPanel>
                            </Popup>
                            <Image Source="{Binding MouseModeImageSourcePath,
                                                    Converter={StaticResource StringResourceConverter}}"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center" />
                        </Grid>
                    </ToggleButton>
                </StackPanel>
            </ToggleButton.Resources>
            <ToggleButton.ContextMenu>
                <ContextMenu>
                    <ContextMenu.Template>
                        <ControlTemplate>
                            <ContentControl Content="{StaticResource AppModeToggleButtonStackPanel}" />
                        </ControlTemplate>
                    </ContextMenu.Template>
                </ContextMenu>
            </ToggleButton.ContextMenu>-->

            <PathIcon Data="{StaticResource MouseSvg}" />
        </ToggleButton>
    </Grid>
</local:MpAvUserControl>
